# D-82 v2.5 Investigate-Goal Scenario Fixture Sub-Sprint

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:verification-before-completion (run commands, read output, THEN make the claim). Steps use checkbox (`- [ ]`) syntax.

**Parent plan:** `docs/superpowers/plans/2026-06-09-v1-to-v4-master-execution-plan.md` § v2.5 Planner/DAG section, and the v5 plan's "scorecard >= 65% unlocks v5 implementation" gate.

**Branch:** `feature/d36-gate2-live` (current). This sub-sprint is committed on top of D-81 (`924f9cb`).

**Goal:** Add 1 new planner integration test for the "investigate goal" scenario (single task, no dependsOn, no tool spec) to push v2.5 from 60% to 65%, **crossing the v5 implementation gate threshold (≥65%)**. The new scenario represents the "investigation" goal type where the planner returns a single atomic task with no further decomposition — a common real-world pattern for "research X" / "look into Y" goals.

---

## 拍板 (Pre-resolved decisions, no further input needed)

1. **Scope:**
   - Modify: `packages/coding-agent/test/integration/tool-loop-policy.test.ts` (add 1 new test)
   - Modify: README.md, ROADMAP.md, docs/ROADMAP_DECISIONS.md, docs/superpowers/v1-v4-evidence-scorecard.{json,md}, docs/superpowers/release-version-hygiene.{json,md}, packages/coding-agent/test/unit/status-doc-hygiene.test.ts, docs/superpowers/v5-v6-planning-preview.{json,md} (mark v5 gate as reached)
   - New plan doc: `docs/superpowers/plans/2026-06-10-d82-v2.5-investigate-scenario.md`
2. **No new code.** The new test uses the same ScriptedLlm + planner fixture pattern as D-77 + D-81. The Planner interface stays untouched.
3. **5 红线 scope:** `packages/coding-agent/src/repl/`, `packages/coding-agent/src/modes/tui.ts`, `packages/coding-agent/src/agent/tool-loop.ts`. Test change is in `tool-loop-policy.test.ts` (not in 5 红线). No source code changes, so 5 红线 diff is guaranteed empty.
4. **Default registry unchanged.**
5. **No npm publish, no tag, no version bump.**
6. **TDD: test-first (RED) → impl-already-exists (GREEN) → bidirectional check.** Move the planner wiring temporarily to verify the new test genuinely exercises it. Restore.
7. **v5 gate reached but not implemented.** v5 implementation is a separate future sub-sprint. This sub-sprint just records that the gate is reached.
8. **No new dependencies.**

---

## Task 1: Add the new test

**File:** `packages/coding-agent/test/integration/tool-loop-policy.test.ts`

**Test:** `'records a single-task investigation plan with no dependencies (D-82 v2.5 investigate scenario)'`

```ts
// User goal: 'investigate why the parser is slow'
// Planner returns 1 task with no dependsOn and no tool spec (atomic investigation).
// Verify the task is recorded into the task graph.
```

Run: `./node_modules/.bin/vitest.cmd run packages/coding-agent/test/integration/tool-loop-policy.test.ts --reporter=verbose`

Expected: 10 original tests (including D-77 + D-80 + 3 D-81) + 1 new = 11 pass.

---

## Task 2: Bidirectional TDD check

```bash
# Temporarily comment out the planner block in tool-loop-policy.ts
# (same approach as D-81)
# Run the test → expect the new test to FAIL
# Restore → expect 11 pass
```

This proves the new test genuinely exercises the planner wiring, not a stale artifact.

---

## Task 3: Update status documents (8 files, atomic)

1. `package.json` — version unchanged (no bump on feature branch)
2. `README.md`, `ROADMAP.md`, `docs/ROADMAP_DECISIONS.md`:
   - `Current sprint: D81 ...` → `Current sprint: D82 v2.5 investigate-goal scenario fixture`
   - Add `D82 v2.5 investigate-goal scenario fixture: 1 new planner integration scenario (single-task investigation, no dependsOn) advances v2.5 to 65% and crosses the v5 implementation gate threshold (>=65%).`
   - Next slice → `D83 v5 implementation kickoff (gated on user direction; v5 plan doc exists at docs/superpowers/plans/2026-06-10-d75-taskgraph-goal-recording.md)`
   - `Last status hygiene sprint: D81.` → `Last status hygiene sprint: D82.`
3. `docs/superpowers/release-version-hygiene.json`:
   - `nextAction` → `D83: v5 implementation kickoff — v2.5 at 65% crosses the v5 gate (>=65%). The v5 plan doc is the implementation roadmap.`
4. `docs/superpowers/v1-v4-evidence-scorecard.json`:
   - `aggregatePercent: 60` → `61`
   - v2.5 `percent: 60` → `65`
   - v2.5 evidence: add `D82 adds 1 investigate-goal scenario (single task, no dependsOn) — brings v2.5 to 65%, crossing the v5 implementation gate threshold.`
   - v2.5 blockers: replace existing blocker with `planner integration has 5 fixtures; v5 implementation gate (v2.5 >= 65%) reached. Remaining gap: a real long-horizon multi-step execution run is still not proven in this scorecard.`
5. `docs/superpowers/v1-v4-evidence-scorecard.md`: mirror JSON changes
6. `packages/coding-agent/test/unit/status-doc-hygiene.test.ts`:
   - `expect(scorecard.aggregatePercent).toBe(60);` → `61`
   - `expect(scorecardMd).toContain('Aggregate evidence-backed progress: 60%');` → `61%`
   - In the 3-doc block test, add D82 line to the completed-slices list
   - Add the new D-82 plan path to the "Plan" listing
   - Add `not.toMatch(/Next implementation slice: D82/i)` to the negation list
   - Add `not.toMatch(/^D82:/m)` to the scorecard.nextActions assertion
7. `docs/superpowers/v5-v6-planning-preview.json`:
   - Add a `gateReached: true` field at the v2.5 level, or add a top-level note about the v5 gate being reached
8. `docs/superpowers/v5-v6-planning-preview.md`: mirror JSON changes

---

## Task 4: Self-test (post-status-update)

```bash
cd D:/App/openClaw/projects/deepwhale
./node_modules/.bin/tsc.cmd -b --pretty false
./node_modules/.bin/eslint.cmd . --max-warnings 0
git diff --check
./node_modules/.bin/vitest.cmd run 2>&1 | tail -5
pnpm.cmd build 2>&1 | tail -5
git diff 924f9cb..HEAD --stat -- packages/coding-agent/src/repl/ packages/coding-agent/src/modes/tui.ts packages/coding-agent/src/agent/tool-loop.ts
```

**Expected:** all 5 commands exit 0; 5 红线 diff is empty.

---

## Task 5: Stage + commit + ship + push

**Stage (explicit files only, NEVER `git add .`):**

```bash
git add \
  packages/coding-agent/test/integration/tool-loop-policy.test.ts \
  packages/coding-agent/test/unit/status-doc-hygiene.test.ts \
  README.md ROADMAP.md docs/ROADMAP_DECISIONS.md \
  docs/superpowers/v1-v4-evidence-scorecard.json \
  docs/superpowers/v1-v4-evidence-scorecard.md \
  docs/superpowers/release-version-hygiene.json \
  docs/superpowers/v5-v6-planning-preview.json \
  docs/superpowers/v5-v6-planning-preview.md \
  docs/superpowers/plans/2026-06-10-d82-v2.5-investigate-scenario.md
```

**Commit 1 (tests + status):**

```bash
git commit -m "test(D-82): v2.5 investigate-goal scenario fixture (v5 gate reached)

- New integration test in tool-loop-policy.test.ts:
  single-task investigation scenario: planner returns 1 task with no
  dependsOn and no tool spec (atomic investigation). Verifies the
  planner correctly records atomic investigation goals.
- All tests RED -> GREEN verified against the existing planner wiring.
  Bidirectional TDD check: temporarily commented out the planner block,
  verified the new test fails with planner off; restored, verified
  it passes with planner on.
- Status blocks advanced: current sprint D-81 -> D-82.
- Scorecard: aggregate 60 -> 61, v2.5 60 -> 65, nextActions still empty.
- v5 implementation gate reached: v2.5 at 65% crosses the >=65% threshold
  defined in the v5 plan (docs/superpowers/plans/2026-06-10-d75-taskgraph-goal-recording.md).
  v5 implementation is a separate future sub-sprint; this commit only
  records that the gate is reached.
- 5 红线 preserved: this sub-sprint is tests-only; no source files
  in 5 红线 scope were touched.
- 1208 -> 1209 pass (delta = +1 new integration test).
- typecheck, lint, build, diff --check all exit 0.
- Default registry unchanged."
```

**Commit 2 (ship marker):**

```bash
git commit --allow-empty -m "ship(coding-agent): D-82 收口 (1 task, 1 commit + 1 ship marker, v2.5 investigate-goal scenario fixture, 1208->1209 pass, scorecard 60->61, v2.5 60->65, v5 implementation gate reached, typecheck/lint/build/diff-check 0, 5 红线 0 改)"
```

**Push:**

```bash
git push origin feature/d36-gate2-live
git ls-remote --heads origin feature/d36-gate2-live
```

---

## Acceptance Criteria Summary

- 1 tests + status commit + 1 ship marker on `feature/d36-gate2-live`
- Test count: 1208 → 1209 / 1 / 4 (delta +1)
- Scorecard: aggregate 60 → 61, v2.5 60 → 65, v5 implementation gate reached
- nextActions still empty
- 5 红线 preserved (empty diff)
- Default registry unchanged
- typecheck/lint/build/diff --check all exit 0
- Branch pushed to `feature/d36-gate2-live`

---

## STOP Conditions

Stop and report to parent (do NOT improvise beyond these):

- Any verification command exits non-zero
- 5 红线 diff is non-empty
- Bidirectional TDD check fails (step 2 passes unexpectedly)
- The new test passes for a different reason (would invalidate RED → GREEN verification)
- Default registry inadvertently changes
- A new dependency is needed (out of scope per §拍板 #8)
