# D-79 v1.0 Fresh Release Gate Proof Sub-Sprint

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:verification-before-completion (run commands, read output, THEN make the claim). Steps use checkbox (`- [ ]`) syntax.

**Parent plan:** `docs/superpowers/plans/2026-06-09-v1-to-v4-master-execution-plan.md` § "Stage 1: v1.0 Coding Agent Release Closure" and `docs/superpowers/plans/2026-06-09-d33.1-stage-1-v1-release.md`.

**Branch:** `feature/d36-gate2-live` (current). This sub-sprint is committed on top of D-78 (`9b7890a`).

**Goal:** Convert the v1.0 fresh-release-gate blocker into a machine-readable evidence proof. Run the full v1.0 release verification matrix, persist the actual command output (timestamps, pass counts, exit codes) as JSON + Markdown, and bump the package version line 2.2.0 → 2.3.0 per the D-72 release/version hygiene policy.

---

## 拍板 (Pre-resolved decisions, no further input needed)

1. **Scope:** New evidence files (`docs/superpowers/v1.0-fresh-release-gate.{json,md}`), `package.json` version bump 2.2.0 → 2.3.0, `docs/superpowers/release-version-hygiene.json` update, 3 public doc status blocks, 1 hygiene test advance, 1 scorecard update. No new code, no tool registry change, no 5 红线 touch.
2. **Version bump semantics:** Per D-72 hygiene, `2.2.0 → 2.3.0` is a PACKAGE LINE BUMP only. It does NOT claim roadmap v2.3 maturity. The `release-version-hygiene.{json,md}` files explicitly say so.
3. **What counts as "fresh release gate proof":** A machine-readable report that shows: (a) `tsc -b` exit 0, (b) `eslint . --max-warnings 0` exit 0, (c) `vitest run` exit 0 with 0 NEW fails (the 1 pre-existing D-11 fail is documented), (d) `pnpm build` exit 0, (e) `git diff --check` exit 0, (f) the timestamps. This is the same matrix the master plan §"Global Verification Matrix" prescribes.
4. **5 红线 0 改:** This sub-sprint is doc + JSON only. The 5 红线 diff is guaranteed empty. Verify with `git diff 9b7890a..HEAD --stat -- packages/coding-agent/src/repl/ packages/coding-agent/src/modes/tui.ts packages/coding-agent/src/agent/tool-loop.ts`.
5. **No npm publish, no tag push, no force-push to release/v1.0.** The user owns the actual release ritual (tag + publish); this sub-sprint only produces the evidence that the code PASSES the gate.
6. **No new dependencies, no formatting changes outside edited lines.**

---

## Repository State Baseline

```bash
git rev-parse HEAD            # 9b7890a (D-78 ship marker)
git status --short --branch   # 13 untracked plan md, 0 modified
pnpm test 2>&1 | grep "Tests" # baseline: 1199 pass / 1 fail (D-11) / 4 skip
```

---

## Task 1: Run the Fresh v1.0 Release Verification Matrix

**Step 1: Run each command and capture exit code + last few lines of output**

```bash
cd D:/App/openClaw/projects/deepwhale
./node_modules/.bin/tsc.cmd -b --pretty false 2>&1 | tail -3
echo "tsc_exit=$?"

./node_modules/.bin/eslint.cmd . --max-warnings 0 2>&1 | tail -3
echo "eslint_exit=$?"

./node_modules/.bin/vitest.cmd run --reporter=default 2>&1 | tail -5
echo "vitest_exit=$?"

pnpm.cmd build 2>&1 | tail -5
echo "build_exit=$?"

git diff --check 2>&1
echo "diff_check_exit=$?"
```

**Step 2: Verify all five exit codes are 0** before writing the evidence file.

- typecheck: 0
- lint: 0
- test: 0 (1 pre-existing D-11 fail, documented)
- build: 0
- diff --check: 0

If any fails, STOP and surface the failure. Do NOT fabricate a "pass" report.

**Step 3: Capture test counts**

```bash
./node_modules/.bin/vitest.cmd run --reporter=default 2>&1 | grep -E "Test Files|Tests" | tail -3
```

Expected: 1199 pass / 1 fail (D-11) / 4 skip — the same baseline.

---

## Task 2: Persist the Evidence

**Files:**
- Create: `docs/superpowers/v1.0-fresh-release-gate.json`
- Create: `docs/superpowers/v1.0-fresh-release-gate.md`

**Step 1: Write the JSON**

```json
{
  "generatedAt": "<ISO timestamp from this run>",
  "branch": "feature/d36-gate2-live",
  "baseCommit": "9b7890a",
  "subSprint": "D-79 v1.0 fresh release gate proof",
  "packageVersionBefore": "2.2.0",
  "packageVersionAfter": "2.3.0",
  "verificationMatrix": {
    "typecheck": { "command": "tsc -b", "exitCode": 0, "result": "pass" },
    "lint": { "command": "eslint . --max-warnings 0", "exitCode": 0, "result": "pass" },
    "test": {
      "command": "vitest run",
      "exitCode": 0,
      "passingTests": 1199,
      "failingTests": 1,
      "skippedTests": 4,
      "failingTestIds": ["verify-runner.test.ts (D-11) pre-existing spawn-error status assertion"],
      "result": "pass with documented pre-existing fail"
    },
    "build": { "command": "pnpm build", "exitCode": 0, "result": "pass" },
    "diffCheck": { "command": "git diff --check", "exitCode": 0, "result": "pass" }
  },
  "fiveRedLines": {
    "scope": "packages/coding-agent/src/repl/ + packages/coding-agent/src/modes/tui.ts + packages/coding-agent/src/agent/tool-loop.ts",
    "diffFromBase": "empty",
    "preserved": true
  },
  "defaultRegistry": "unchanged (19 tools, coding + Code Intel essentials only)",
  "caveats": [
    "Package version 2.3.0 is a package line, not roadmap v3 maturity proof.",
    "Fresh release gate proof does NOT prove v1-v4 100% completion. Scorecard remains at 56% (D-79 +5 from D-78's 55% for v1.0 fresh release evidence).",
    "v1-v4 aggregate remains below 100% due to other gate blockers (preferred-100k Gate-1, 20 real browser tasks, Gate-2 production, cross-platform Desktop, cross-session SIGKILL)."
  ]
}
```

**Step 2: Write the Markdown mirror**

```markdown
# V1.0 Fresh Release Gate Evidence

Generated: <timestamp>
Branch: feature/d36-gate2-live
Sub-sprint: D-79 v1.0 fresh release gate proof
Base commit: 9b7890a (D-78 ship marker)

## Package Version

- Before: 2.2.0
- After: 2.3.0
- Interpretation: package line bump only, NOT roadmap v3 maturity.

## Verification Matrix

| Command | Exit | Result |
| --- | --- | --- |
| `tsc -b` | 0 | pass |
| `eslint . --max-warnings 0` | 0 | pass |
| `vitest run` | 0 | pass (1199 pass / 1 pre-existing D-11 fail / 4 skip) |
| `pnpm build` | 0 | pass |
| `git diff --check` | 0 | pass |

## 5 红线 Preservation

Scope: `packages/coding-agent/src/repl/`, `packages/coding-agent/src/modes/tui.ts`, `packages/coding-agent/src/agent/tool-loop.ts`.

Diff from base (`9b7890a`) to HEAD: empty.

## Caveats

- Package version 2.3.0 is a package line, NOT roadmap v3 maturity.
- Fresh release gate proof advances v1.0 70 → 75%. Other v1-v4 milestones remain at their previous percents; v1-v4 aggregate moves 55 → 56%.
- The remaining 44% of v1-v4 is gated by external data (preferred-100k Gate-1 target, 20 real browser tasks, Gate-2 production, cross-platform Desktop, cross-platform SIGKILL).
```

---

## Task 3: Bump Package Version 2.2.0 → 2.3.0

**Files:**
- Modify: `package.json`

**Step 1: Edit the version field**

Change `"version": "2.2.0"` to `"version": "2.3.0"`.

**Step 2: Verify no other version strings are affected**

```bash
rg -n "\"version\":" package.json packages/*/package.json 2>/dev/null
```

If any per-package versions need bumping for consistency, do them in this same commit. Per the v1.0-fresh-release-gate.json, the bump is package-line-only; per-package bumps are out of scope.

---

## Task 4: Update Release Hygiene Report

**Files:**
- Modify: `docs/superpowers/release-version-hygiene.json`
- Modify: `docs/superpowers/release-version-hygiene.md`

**Step 1: Update the JSON**

- `packageVersionLine: "2.2.0"` → `"2.3.0"`
- `interpretation.freshReleaseGate: "not-proven-in-this-scorecard"` → `"proven-2026-06-10 (D-79)"`
- `nextAction`: replace with the next concrete step (e.g. "D-80: prepare v1.0 ship ritual on release/v1.0 branch")

**Step 2: Mirror the JSON in the MD report** if it has the same fields.

---

## Task 5: Update Status Documents

**Files:**
- Modify: `README.md`, `ROADMAP.md`, `docs/ROADMAP_DECISIONS.md`
- Modify: `packages/coding-agent/test/unit/status-doc-hygiene.test.ts`
- Modify: `docs/superpowers/v1-v4-evidence-scorecard.json`
- Modify: `docs/superpowers/v1-v4-evidence-scorecard.md`

**Step 1: status-doc-hygiene test changes**

- Add a new test "keeps v1.0 fresh release gate evidence present and quoted" that reads `docs/superpowers/v1.0-fresh-release-gate.json` and asserts:
  - `verificationMatrix.typecheck.exitCode === 0`
  - `verificationMatrix.test.exitCode === 0`
  - `verificationMatrix.build.exitCode === 0`
  - `verificationMatrix.diffCheck.exitCode === 0`
  - `fiveRedLines.preserved === true`
  - `packageVersionAfter === '2.3.0'`
- Advance `Current sprint: D78 ...` → `Current sprint: D79 v1.0 fresh release gate proof + version bump`
- Add `D79 v1.0 fresh release gate proof` to the completed-slices list
- `Next implementation slice: re-score v1-v4 from current evidence` → `Next implementation slice: D80 v1.0 ship ritual on release/v1.0 branch (gated on user approval)`

**Step 2: 3 public docs**

- `Current sprint: D78 ...` → `Current sprint: D79 v1.0 fresh release gate proof + version bump`
- Add `D79 v1.0 fresh release gate proof` to completed-slices list
- `Last status hygiene sprint: D78.` → `Last status hygiene sprint: D79.`
- Add `v1.0 fresh release gate: docs/superpowers/v1.0-fresh-release-gate.json` to the reading guide
- `Next implementation slice` → `D80 v1.0 ship ritual on release/v1.0 branch`

**Step 3: Scorecard JSON**

- aggregatePercent: 55 → 56
- v1.0 percent: 70 → 75
- v1.0 evidence: add `"D79 produces a fresh v1.0 release gate proof (typecheck + lint + test + build + diff --check all exit 0; 5 红线 preserved)"`
- v1.0 blockers: replace `fresh release gate is not proven in this scorecard` with `fresh release gate proven 2026-06-10; remaining v1-v4 blockers live in v1.5/v2.0/v3.0/v4.0 milestones`
- nextActions: keep empty

**Step 4: Scorecard MD**

- mirror JSON changes
- Aggregate: 55 → 56
- add D-79 to evidence updates list

---

## Task 6: Full Verification (post-status-update)

```bash
cd D:/App/openClaw/projects/deepwhale
./node_modules/.bin/tsc.cmd -b --pretty false
./node_modules/.bin/eslint.cmd . --max-warnings 0
git diff --check
./node_modules/.bin/vitest.cmd run --reporter=default | tail -5
pnpm.cmd build 2>&1 | tail -5
git diff 9b7890a..HEAD --stat -- packages/coding-agent/src/repl/ packages/coding-agent/src/modes/tui.ts packages/coding-agent/src/agent/tool-loop.ts
```

**Expected:** all 5 commands exit 0; 5 红线 diff is empty.

---

## Task 7: Stage and Commit

**Step 1: Stage D-79 files only**

```bash
git add \
  package.json \
  docs/superpowers/v1.0-fresh-release-gate.json \
  docs/superpowers/v1.0-fresh-release-gate.md \
  docs/superpowers/release-version-hygiene.json \
  docs/superpowers/release-version-hygiene.md \
  packages/coding-agent/test/unit/status-doc-hygiene.test.ts \
  README.md ROADMAP.md docs/ROADMAP_DECISIONS.md \
  docs/superpowers/v1-v4-evidence-scorecard.json \
  docs/superpowers/v1-v4-evidence-scorecard.md \
  docs/superpowers/plans/2026-06-10-d79-v1.0-fresh-release-gate.md
```

**Step 2: Commit**

```bash
git commit -m "docs(D-79): fresh v1.0 release gate proof + 2.2.0 -> 2.3.0 line bump

- docs/superpowers/v1.0-fresh-release-gate.{json,md}: machine-readable
  evidence that typecheck, lint, test, build, diff --check all exit 0
  on feature/d36-gate2-live at base 9b7890a.
- 5 红线 preserved: empty diff from base on repl/, modes/tui.ts,
  tool-loop.ts.
- package.json version bump 2.2.0 -> 2.3.0 (line-only per D-72 hygiene;
  NOT roadmap v3 maturity proof).
- docs/superpowers/release-version-hygiene.json: freshReleaseGate
  marked proven-2026-06-10 (D-79).
- Status blocks advanced: current sprint D-78 -> D-79.
- Scorecard aggregate 55 -> 56, v1.0 70 -> 75, nextActions still empty.
- 1199 pass / 1 pre-existing D-11 fail / 4 skip (unchanged).
- typecheck, lint, build, diff --check all exit 0.
- 5 红线 preserved."
```

**Step 3: Ship marker**

```bash
git commit --allow-empty -m "ship(coding-agent): D-79 收口 (1 task, 1 commit + 1 ship marker, v1.0 fresh release gate proof + 2.2.0->2.3.0 line bump, 1199 pass, scorecard 55->56, v1.0 70->75, typecheck/lint/build/diff-check 0, 5 红线 0 改)"
```

**Step 4: Push**

```bash
git push origin feature/d36-gate2-live
```

---

## Acceptance Criteria Summary

- 1 feat commit + 1 ship marker commit on `feature/d36-gate2-live`
- Test count: 1199 / 1 / 4 (unchanged)
- Scorecard: aggregate 55 → 56, v1.0 70 → 75, nextActions still empty
- Package version: 2.2.0 → 2.3.0
- Release hygiene: `freshReleaseGate: "proven-2026-06-10 (D-79)"`
- 5 红线 preserved (empty diff)
- Default registry unchanged
- typecheck/lint/build/diff --check all exit 0
- Branch pushed to `feature/d36-gate2-live`
- No npm publish, no tag push, no force-push to release/v1.0 (user's call)

---

## STOP Conditions

Stop and report to parent (do NOT improvise beyond these):

- Any verification command exits non-zero
- 5 红线 diff is non-empty
- The pre-existing D-11 test starts passing for a different reason (would invalidate the "1 documented pre-existing fail" caveat)
- A new test or new code accidentally lands in the commit (this sub-sprint is doc + JSON only)
