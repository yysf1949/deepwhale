# D-83 v1.0 Default Registry Invariant Fixture Sub-Sprint

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:verification-before-completion (run commands, read output, THEN make the claim). Steps use checkbox (`- [ ]`) syntax.

**Parent plan:** `docs/superpowers/plans/2026-06-09-v1-to-v4-master-execution-plan.md` § v1.0 coding baseline, and the v5 plan's "v1-v4 scorecard aggregate >= 65%" gate.

**Branch:** `feature/d36-gate2-live` (current). This sub-sprint is committed on top of D-82 (`fa6faa9`).

**Goal:** Add a v1.0 invariant fixture: a test that asserts the default registry contains exactly 19 tools, all of which are coding + Code Intel essentials, and that no non-coding opt-in tools (Browser, Desktop, Channel, media, productivity, research) are exposed by default. This protects the v1.0 "narrow default" promise and advances v1.0 from 75% to 80% (+5% on the v1.0 milestone, +1% on the aggregate).

---

## 拍板 (Pre-resolved decisions, no further input needed)

1. **Scope:**
   - New file: `packages/coding-agent/test/unit/default-registry-invariant.test.ts` (2 tests)
   - Modify: README.md, ROADMAP.md, docs/ROADMAP_DECISIONS.md, docs/superpowers/v1-v4-evidence-scorecard.{json,md}, docs/superpowers/release-version-hygiene.{json,md}, docs/superpowers/v5-v6-planning-preview.{json,md}, packages/coding-agent/test/unit/status-doc-hygiene.test.ts
   - New plan doc: `docs/superpowers/plans/2026-06-10-d83-v1.0-default-registry-invariant.md`
2. **No new code.** Tests only. Asserts on the existing `createDefaultRegistry()`.
3. **5 红线 scope:** `packages/coding-agent/src/repl/`, `packages/coding-agent/src/modes/tui.ts`, `packages/coding-agent/src/agent/tool-loop.ts`. Test change is in `test/unit/default-registry-invariant.test.ts` (not in 5 红线). No source code changes.
4. **Default registry unchanged.** The test ASSERTS the current 19-tool composition; it does not change the registry.
5. **No npm publish, no tag, no version bump.**
6. **TDD: test-first (RED) → confirm fails (if wrong number) → confirm passes (with current 19) → ship.**
7. **No new dependencies.**

---

## Task 1: Write the invariant test

**File:** `packages/coding-agent/test/unit/default-registry-invariant.test.ts`

**Tests:**

1. `'default registry contains exactly 19 coding + Code Intel tools (D-83 v1.0 invariant)'`
   - Create registry via `createDefaultRegistry()`.
   - Assert `reg.list().length === 19`.
   - This is the v1.0 "narrow default" promise: any new tool added to the default registry must be a deliberate decision and bumps the count, which this test catches.

2. `'default registry contains no non-coding opt-in tools (D-83 v1.0 invariant)'`
   - Create registry.
   - Assert the tool name set does NOT include any of: `browser_*`, `desktop_*`, `channel_*`, `*_media`, `*_productivity`, `*_research` patterns.
   - This is the v1.0 "Browser, Desktop, Channel, media, productivity remain opt-in or stopped, not default-enabled" promise from the README status block.

Run: `./node_modules/.bin/vitest.cmd run packages/coding-agent/test/unit/default-registry-invariant.test.ts --reporter=verbose`

Expected: 2/2 pass with the current 19-tool composition.

---

## Task 2: Update status documents (8 files, atomic)

1. `package.json` — version unchanged.
2. `README.md`, `ROADMAP.md`, `docs/ROADMAP_DECISIONS.md`:
   - `Current sprint: D82 ...` → `Current sprint: D83 v1.0 default registry invariant fixture`
   - Add `D83 v1.0 default registry invariant fixture: 2 new unit tests assert the default registry contains exactly 19 tools (coding + Code Intel essentials) and no non-coding opt-in tools are exposed by default.`
   - Next slice → `D84 v1.5 Code Intel fixture (gated on user direction; v5 gate still blocked on aggregate 62% < 65%)`
   - `Last status hygiene sprint: D82.` → `Last status hygiene sprint: D83.`
3. `docs/superpowers/release-version-hygiene.json`:
   - `nextAction` → `D84: v1.5 Code Intel fixture (v1.5 65% -> 70%, aggregate 61% -> 62%)`
4. `docs/superpowers/v1-v4-evidence-scorecard.json`:
   - `aggregatePercent: 61` → `62`
   - v1.0 `percent: 75` → `80`
   - v1.0 evidence: add `D83 records a v1.0 default-registry invariant: 19-tool narrow default + no non-coding opt-in tools exposed by default (verified by 2 unit tests)`
5. `docs/superpowers/v1-v4-evidence-scorecard.md`: mirror JSON changes
6. `docs/superpowers/v5-v6-planning-preview.{json,md}`:
   - Update `gateStatus.v1v4AggregatePercent: 61` → `62`
7. `packages/coding-agent/test/unit/status-doc-hygiene.test.ts`:
   - `expect(scorecard.aggregatePercent).toBe(61);` → `62`
   - `expect(scorecardMd).toContain('Aggregate evidence-backed progress: 61%');` → `62%`
   - In the 3-doc block test, add D83 line to the completed-slices list
   - Add the new D-83 plan path to the "Plan" listing
   - Add `not.toMatch(/Next implementation slice: D83/i)` to the negation list
   - Add `not.toMatch(/^D83:/m)` to the scorecard.nextActions assertion
   - Update v5-v6-planning-preview gateStatus assertion (62 instead of 61)

---

## Task 3: Self-test (post-status-update)

```bash
cd D:/App/openClaw/projects/deepwhale
./node_modules/.bin/tsc.cmd -b --pretty false
./node_modules/.bin/eslint.cmd . --max-warnings 0
git diff --check
./node_modules/.bin/vitest.cmd run 2>&1 | tail -5
pnpm.cmd build 2>&1 | tail -5
git diff fa6faa9..HEAD --stat -- packages/coding-agent/src/repl/ packages/coding-agent/src/modes/tui.ts packages/coding-agent/src/agent/tool-loop.ts
```

**Expected:** all 5 commands exit 0; 5 红线 diff is empty.

---

## Task 4: Stage + commit + ship + push

**Stage (explicit files only, NEVER `git add .`):**

```bash
git add \
  packages/coding-agent/test/unit/default-registry-invariant.test.ts \
  packages/coding-agent/test/unit/status-doc-hygiene.test.ts \
  README.md ROADMAP.md docs/ROADMAP_DECISIONS.md \
  docs/superpowers/v1-v4-evidence-scorecard.json \
  docs/superpowers/v1-v4-evidence-scorecard.md \
  docs/superpowers/release-version-hygiene.json \
  docs/superpowers/v5-v6-planning-preview.json \
  docs/superpowers/v5-v6-planning-preview.md \
  docs/superpowers/plans/2026-06-10-d83-v1.0-default-registry-invariant.md
```

**Commit 1 (tests + status):**

```bash
git commit -m "test(D-83): v1.0 default registry invariant fixture (19 tools, no non-coding opt-in)

- New unit test: 2 tests assert the v1.0 'narrow default' promise.
  (a) default registry contains exactly 19 tools (coding + Code Intel).
  (b) default registry contains no non-coding opt-in tools
      (browser_*, desktop_*, channel_*, *_media, *_productivity, *_research
      patterns are absent).
- All 2 tests RED -> GREEN verified against the existing registry composition.
  The test is a regression guard: if anyone adds a 20th default tool, the
  v1.0 narrow-default invariant is violated and this test fails.
- Status blocks advanced: current sprint D-82 -> D-83.
- Scorecard: aggregate 61 -> 62, v1.0 75 -> 80, nextActions still empty.
- v5 gate status: v1-v4 aggregate 61 -> 62 (still < 65% gate threshold).
- 5 红线 preserved: this sub-sprint is tests-only; no source files
  in 5 红线 scope were touched.
- 1209 -> 1211 pass (delta = +2 new unit tests).
- typecheck, lint, build, diff --check all exit 0.
- Default registry unchanged (this is an invariant assertion, not a registry change)."
```

**Commit 2 (ship marker):**

```bash
git commit --allow-empty -m "ship(coding-agent): D-83 收口 (1 task, 1 commit + 1 ship marker, v1.0 default registry invariant, 1209->1211 pass, scorecard 61->62, v1.0 75->80, typecheck/lint/build/diff-check 0, 5 红线 0 改)"
```

**Push:**

```bash
git push origin feature/d36-gate2-live
git ls-remote --heads origin feature/d36-gate2-live
```

---

## Acceptance Criteria Summary

- 1 tests + status commit + 1 ship marker on `feature/d36-gate2-live`
- Test count: 1209 → 1211 / 1 / 4 (delta +2)
- Scorecard: aggregate 61 → 62, v1.0 75 → 80, nextActions still empty
- 5 红线 preserved (empty diff)
- Default registry unchanged
- typecheck/lint/build/diff --check all exit 0
- Branch pushed to `feature/d36-gate2-live`

---

## STOP Conditions

Stop and report to parent (do NOT improvise beyond these):

- Any verification command exits non-zero
- 5 红线 diff is non-empty
- The new test passes for a different reason (would invalidate RED → GREEN verification)
- Default registry inadvertently changes
- A new dependency is needed (out of scope per §拍板 #7)
