{
  "generatedAt": "2026-06-11T00:00:00.000Z",
  "basis": {
    "branch": "feature/d36-gate2-live",
    "packageVersionLine": "2.3.0",
    "gate1PreferredReport": "docs/superpowers/gate-1-preferred-targets.json",
    "gate2LiveReport": "docs/superpowers/gate-2-long-horizon-live.json",
    "masterPlan": "docs/superpowers/plans/2026-06-09-v1-to-v4-master-execution-plan.md"
  },
  "aggregatePercent": 65,
  "milestones": [
    {
      "id": "v1.0",
      "percent": 80,
      "status": "Mostly implemented coding baseline; fresh release gate proven 2026-06-10 (D-79); default registry invariant asserted (D-83)",
      "evidence": [
        "coding tool registry baseline exists",
        "core session and tool-loop tests exist",
        "public docs explicitly avoid production-complete claims",
        "D72 release/version hygiene report distinguishes package version from roadmap maturity",
        "D79 produces a fresh v1.0 release gate proof (typecheck + lint + test + build + diff --check all exit 0; 5 红线 preserved)",
        "D83 records a v1.0 default-registry invariant: 19-tool narrow default + no non-coding opt-in tools exposed by default (verified by 2 unit tests)"
      ],
      "blockers": [
        "package version 2.3.0 is a package line, not roadmap v2.3 maturity proof",
        "no new public code claims; the v1.0 ship ritual (tag, npm publish) is gated on user approval"
      ]
    },
    {
      "id": "v1.5",
      "percent": 70,
      "status": "Code Intel foundation exists and is labeled heuristic; preferred 100K Gate-1 evidence is blocked; re-export chain call graph correctness fixture present (D-84)",
      "evidence": [
        "symbol graph, reference graph, heuristic call graph, and Code Intel tools exist",
        "Gate-1 minimum Vite evidence is present at 86216 supported LOC",
        "Code Intel descriptions and metadata label heuristic behavior",
        "D67 rename_symbol exposes hashline edit hunks and heuristic confidence metadata",
        "D69 refreshed Gate-1 target inventory and Vite minimum-50k Gate-1 scenario evidence",
        "D71 covers TypeScript combined default-plus-named import references and call edges",
        "D74 resolves TypeScript default re-export barrel call edges to the original named default declaration",
        "D84 records a v1.5 Code Intel re-export chain call-graph fixture: the heuristic correctly recognizes transitive callers across a caller -> intermediate re-exporter -> target boundary (verified by 1 new test in call-graph.test.ts)"
      ],
      "blockers": [
        "preferred-100k Gate-1 evidence is blocked by missing local 100K+ target",
        "rename_symbol is reference-guided lexical rewrite with hashline edit hunks, not IDE-grade AST rename"
      ]
    },
    {
      "id": "v2.0",
      "percent": 40,
      "status": "memory, Browser, and MCP foundations exist as opt-in or early pieces; Gate-1.5 live task sourcing and opt-in runner boundary exist, but live result evidence is incomplete",
      "evidence": [
        "memory modules exist",
        "Browser foundation modules exist behind opt-in surfaces",
        "MCP package and runtime foundation exist",
        "D70 refreshed Gate-1.5 fixture evidence with binding=false and branchDecision=defer-live-evidence",
        "D73 live Browser task ledger records 0/20 live tasks and binding=false",
        "D114 queues 20 pending candidate live Browser tasks as sourcing evidence only; 0/20 completed live results exist and binding remains false",
        "D115 adds an opt-in live Browser task runner boundary; it runs only with optIn=true plus an injected adapter, and still leaves 0/20 completed live results"
      ],
      "blockers": [
        "Gate-1.5 live 20-task browser result evidence is not complete",
        "default registry must remain coding plus Code Intel essentials"
      ]
    },
    {
      "id": "v2.5",
      "percent": 65,
      "status": "planner/DAG/cache modules exist; main-loop integration has multi-scenario planner.plan evidence fixtures (D-77 + 3 D-81 + D-82); v5 implementation gate reached (v2.5 >= 65%)",
      "evidence": [
        "planner, task DAG, and plan cache modules exist",
        "module-level tests exist for planner foundations",
        "D75 records latest user goals into TaskGraphRecorder through runToolLoopWithReview",
        "D77 records planner.plan invocation in the main loop with the latest user goal when a Planner is provided",
        "D81 adds 3 multi-scenario planner integration tests: multi-task DAG with dependsOn dependencies, tool spec preservation, and negative test confirming planner is gated by goal presence",
        "D82 adds 1 investigate-goal scenario (single task, no dependsOn) -- brings v2.5 to 65%, crossing the v5 implementation gate threshold"
      ],
      "blockers": [
        "planner integration has 5 fixtures; a real long-horizon multi-step execution run is still not proven in this scorecard",
        "release gate scenarios are not freshly proven in this scorecard"
      ]
    },
    {
      "id": "v3.0",
      "percent": 55,
      "status": "Reviewer and Gate-2 runner exist; current Gate-2 live fixture passes under default profile; inclusive 30-50 boundary fixture present (D-85)",
      "evidence": [
        "Reviewer modules exist",
        "Gate-2 live evidence is passed_live=true with toolCalls=31 and registryProfile=default",
        "D85 records a v3.0 Gate-2 long-horizon boundary fixture: 2 new tests assert the inclusive 30-50 tool-call range at exact boundaries (30 and 50) and just outside (29 and 51)"
      ],
      "blockers": [
        "Gate-2 evidence is a default-profile invoice fixture pass, not broad long-horizon production proof",
        "Gate-1.5 binding branch decision remains deferred pending live browser tasks"
      ]
    },
    {
      "id": "v4.0",
      "percent": 50,
      "status": "Researcher, TaskGraph, memory, and channel foundations exist; cross-session memory crash/reload evidence plus multi-hop cross-session TaskGraph persistence evidence fixtures present",
      "evidence": [
        "Researcher and TaskGraph modules exist",
        "persistent memory and channel foundation modules exist",
        "D78 records atomic write semantics plus partial-last-line recovery for the persistent memory store",
        "D80 records cross-session TaskGraph persistence: a second PersistingTaskGraphRecorder instance loaded from the same file sees the first instance's records (D-78 storage layer + D-80 Agent OS layer)",
        "D86 records multi-hop cross-session handoff: 3 instances (A writes 3, B writes 2, C reads all 5 in order) verify the JSONL format supports Agent OS session handoffs with multiple intermediate instances"
      ],
      "blockers": [
        "Agent OS orchestration is not production-complete",
        "Desktop and channels remain stopped or opt-in, not default-enabled",
        "cross-session crash recovery evidence is deterministic unit-style fixtures (D-78 + D-80 + D-86), not real cross-platform SIGKILL tests"
      ]
    }
  ],
  "caveats": [
    "Gate-2 default-profile fixture pass is not v1-v4 production completion.",
    "Gate-1 minimum-50k evidence is not preferred-100k evidence.",
    "Module existence is not production integration.",
    "Code Intel rename, reference, call graph, and smart search behavior remains heuristic unless a specific test proves stronger semantics.",
    "Default registry exposure remains coding plus Code Intel essentials; non-coding surfaces require explicit opt-in."
  ],
  "nextActions": [
    "D116: record opt-in Gate-1.5 Browser runner results into the live task ledger without unlocking Browser defaults until 20 completed results exist.",
    "Continue preferred-100k Gate-1 search only when a local 100K+ target is available.",
    "Keep Gate-2 production, cross-platform Desktop, and cross-platform SIGKILL evidence as separate future blockers rather than inferring them from unit fixtures."
  ]
}
