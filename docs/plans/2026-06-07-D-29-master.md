# D-29: v1.0.13 — 3 个 P1 清理 (repl.ts 拆 + secret redact + tui-ink 组件测)

**Sprint**: 1c-revive-5-D-29 (2026-06-07)
**Ship Ritual**: 1 主 plan + 3 sub-plan + 3 commit cluster (D-29.1 / D-29.2 / D-29.3) + 1 docs 同步
**起点**: `superpowers` 全面审查 (2026-06-07) 的 6 份并行 review 报告
**用户拍板** (2026-06-07): A → B 串行, C (本 plan) 并行

## 1. 范围 — 3 个独立子系统 (3 sub-plan, 3 commit cluster)

D-29 不做 1 大拆, 拆成 3 个**互不阻塞**的子计划。每个 sub-plan 自包含 working software + tests + docs。3 个 sub-plan 可在同 sprint 内顺序或并行 ship (任一 sub-plan ship 失败不影响另外 2 个)。

| Sub-plan | 范围 | 文件数 | 测试增 | 风险 | 路径 |
|---|---|---|---|---|---|
| D-29.1 | `repl.ts` god-file 拆分 (主审 §1.4 Arch-1) | 4 新 + 1 改 | 0 改 (现有 6/6 仍绿) | 中 (高 LOC 改动) | `docs/plans/2026-06-07-D-29.1-repl-split.md` |
| D-29.2 | `policy/sanitize-reason.ts` secret redact (主审 §4.2 SEC-3) | 1 改 | +6 it (8 → 14) | 低 (单文件) | `docs/plans/2026-06-07-D-29.2-sanitize-secret.md` |
| D-29.3 | tui-ink 4 组件 ink-testing-library 单测 (主审 §3.3 TC-2) | 4 新 | +12-16 it | 低 (加测, 不改 prod) | `docs/plans/2026-06-07-D-29.3-tui-ink-component-test.md` |

## 2. 不动 (红线)

- **现有 6 红线 (D-19 / D-19.5 / D-19.5p / 6afccc8 / 1ceef94)**: 任何 sub-plan 不得触碰。`repl.ts` 拆完重组必须 0 红线漂移。
- **`bin/deepwhale.js`**: D-29.1 重组 repl 时, import 路径不变 (`from './repl.js'` 仍导出 `startRepl` + `runAgentTurn`)。
- **`createReplConfirm` / `staticToolPolicy` / `formatUsageStatus` / `SessionReader` / `runToolLoop`**: D-29.1 抽函数时, 这些 imports 仅位置变, 不增不减。
- **测试基线**: 705 passed / 11 skipped (main @ 3ad8b39) 必须 0 regression。子 plan 完成后数字增加, 但不允许减少。
- **`policy/sanitize-reason.ts` 公开签名**: D-29.2 不改 `sanitizeReason(input: string): string` 签名, 仅在内部新增 redact 步骤。
- **tui-ink 8 组件 + 8 hooks 接口**: D-29.3 只加测, 不改任何组件/hooks 的 props 形状、导出名称、state 形状。

## 3. Ship Sequence (推荐顺序)

```
D-29.1 (repl.ts 拆, 风险中)    ──┐
                                  ├──► D-29 ship 总集 (v1.0.13 bump)
D-29.2 (secret redact, 风险低)  ──┤
                                  │
D-29.3 (tui-ink 测, 风险低)     ──┘
```

**推荐顺序**: D-29.2 → D-29.3 → D-29.1
- 理由 1: D-29.2 + D-29.3 都是低风险小改, 先 ship 累计 commit 经验, 给 D-29.1 拆 repl 这种高 LOC 改动铺路
- 理由 2: D-29.1 拆 repl 时需重跑 `repl-shared-stdin.test.ts` 6/6 + 全 coding-agent 305 passed, 先 ship 2 个 sub-plan 后基线更稳
- 理由 3: D-29.1 拆完后的新 `ReplSession` / `ReplCommandRouter` / `ReplSignalCoordinator` 边界清晰, 给后续 D-30+ (功能扩展) 留接口

可并行 (子 agent 隔离 OK): D-29.2 + D-29.3 同时派 2 个 subagent, D-29.1 串行 (避免 2 个 subagent 同时改 `repl.ts`)

## 4. 1 plan + 3 commit cluster + 1 docs 同步 (用户 ship ritual)

每 sub-plan:
- 1 plan 文档 (本目录内)
- 1 commit cluster (D-29.1 = 4 颗 / D-29.2 = 1 颗 / D-29.3 = 4 颗, 跟 §1 表格对齐)
- 1 docs/README 同步 (D-29 总集收尾时一次性同步, 不每 sub-plan 同步)

D-29 总集 ship:
- bump 5 packages 1.0.12 → 1.0.13
- README § capability matrix 同步
- ROADMAP § v1.0.13 行加 (跟 D-28 ship 仪式一致)
- 飞书推送通知 (用户协议)

## 5. 验收标准 (D-29 总集 ship 门槛)

- [ ] 3 个 sub-plan 文档完成 (本目录)
- [ ] 3 个 sub-plan commit cluster 各 ship 进 main (3 sub-PR, 或 1 大 PR 含 3 cluster — 用户拍板)
- [ ] `pnpm typecheck` 0 错
- [ ] `pnpm lint --max-warnings 0` 0 warning
- [ ] `pnpm exec vitest run` 全绿, 测数 ≥ 705+12+6+14 (D-29.1 不增测 / D-29.2 +6 / D-29.3 +12 估算保守下限)
- [ ] 5 红线 (D-19 / D-19.5 / D-19.5p / 6afccc8 / 1ceef94) 逐条 re-grep 验保留
- [ ] tui-ink 8 组件 + 8 hooks 100% 有 ink-testing-library 单测 (D-29.3 完成后)
- [ ] sanitize-reason 单测覆盖 3 类 secret 形式 (sk-* / Bearer * / key=*)
- [ ] repl.ts ≤ 300 行 (从 947 行降), startRepl ≤ 250 行 (从 478 行降)
- [ ] 飞书推送通知带 `git ls-remote` 远端证据 (用户协议)

## 6. 不在范围 (defer)

- **D-30+ 候选**: `modes/tui.ts` 删除 (LEGACY) / `verify-runner.ts` 拆 (主审 §1.4 Arch-5) / `repl.ts:483-485` no-op removeEventListener 删 (主审 §2.4 CQ-3) / `docker-runner.ts:44` _MAX_BUFFER_UNUSED 删 (主审 §2.4 CQ-1) — 都是 P2, 留 D-30+
- **D-29.4 候选**: README "99% 命中" 标 P2 (主审 §5.1 #1, "缺端到端 cache 命中测"), 跟 D-20.2 P1 留 sprint 2 一致
- **D-29.5 候选**: tui-ink `app.tsx:159` placeholder user entry (d-20.6 review §1.3 F4), P2 nice-to-have

## 7. Sub-plan 链接 (子文档)

1. `docs/plans/2026-06-07-D-29.1-repl-split.md` — `repl.ts` 947L → ≤300L, 抽 ReplSession / ReplCommandRouter / ReplSignalCoordinator
2. `docs/plans/2026-06-07-D-29.2-sanitize-secret.md` — `sanitize-reason.ts` 加 3 类 secret redact, 测 8 → 14 it
3. `docs/plans/2026-06-07-D-29.3-tui-ink-component-test.md` — StatusBar / Divider / Prompt / Confirm 4 组件 ink-testing-library 单测, 测 +12-16 it
