# D-30.5 D-30 核心收口 Plan — 7 missing items + help text (2026-06-08)

> **For opencode implementer:** Use TDD throughout. Write failing test first.
> Run, confirm fail, then implement. Commit after each green test.
> **拍板 (assistant 2026-06-08):** D-30.4 ship (fa65bf0) 后盘点 7 项 核心 plan 未实装
> (Mermaid 渲染 + 5 UI 组件 + 1 skill + help text), 本批 ~15 commit, ~45 min。

**Goal:** Close 7 remaining 核心 items in D-30 master plan: Mermaid rendering (G1), 5
UI components (C3-C7), requesting-code-review skill (L1), and update /help text to
list all 14 slash commands. Bump to 1.0.15 (post 1.0.14 ship) and ship.

**Architecture:**
- Mermaid: ASCII rendering via minimal hand-rolled parser (subset: box, arrow, label)
- UI components: Ink-based React components in `packages/tui-ink/src/components/`,
  follow PlanView/TodoList pattern (useInk-testing-library for tests)
- Code review skill: `packages/coding-agent/src/skills/requesting-code-review.ts`
  (engine module, not Hermes-skill format — Hermes skills live in ~/.hermes/skills/)
- Help text: update `dispatchSlashBuiltin` /help case in `repl-command-router.ts`
- Bump 5 packages 1.0.14 → 1.0.15

**Tech Stack:** TypeScript, vitest, ink-testing-library, React 18 (for tui-ink components)

---

## Task 1: Mermaid → ASCII renderer (D-30.5.1)

**Files:**
- Create: `packages/coding-agent/src/util/mermaid-render.ts`
- Test: `packages/coding-agent/test/unit/mermaid-render.test.ts`

**Step 1.1: Write failing test** (4 形状: box, arrow, label, decision)
```ts
import { describe, it, expect } from 'vitest';
import { renderMermaid } from '../../src/util/mermaid-render.js';

describe('renderMermaid', () => {
  it('renders simple graph box + arrow', () => {
    const out = renderMermaid('graph LR\n  A[Start] --> B[End]');
    expect(out).toContain('A');
    expect(out).toContain('B');
    expect(out).toContain('──▶');
  });
  it('renders with labels on arrows', () => {
    const out = renderMermaid('graph LR\n  A -->|yes| B');
    expect(out).toContain('yes');
  });
  it('renders decision diamond', () => {
    const out = renderMermaid('graph TD\n  X{OK?} -->|yes| Y');
    expect(out).toContain('X');
    expect(out).toContain('OK?');
  });
  it('returns fallback for empty input', () => {
    expect(renderMermaid('')).toContain('(empty diagram)');
  });
});
```

**Step 1.2: Run test — confirm it fails**
Command: `cd packages/coding-agent && pnpm exec vitest run test/unit/mermaid-render.test.ts`
Expected: FAIL — module not found

**Step 1.3: Implement** (subset: 3 shapes + arrow, ASCII output)
```ts
// packages/coding-agent/src/util/mermaid-render.ts
export function renderMermaid(src: string): string {
  const trimmed = src.trim();
  if (!trimmed) return '(empty diagram)';
  const lines = trimmed.split('\n').filter(l => !l.startsWith('graph') && !l.startsWith('flowchart'));
  const nodes = new Map<string, { label: string; shape: 'box' | 'diamond' | 'circle' }>();
  const edges: Array<[string, string, string?]> = [];
  for (const line of lines) {
    const m = line.match(/(\w+)\s*[\[{](\w[^\]}{]*)[\]}](?:\{(\w[^}]*)\})?\s*-->?\|?([^|]*)\|?\s*(\w+)\s*[\[{]?(\w[^\]}{]*)?[\]}]?/);
    if (m) {
      const [, from, fromLabel, , label, to, toLabel] = m;
      const fromShape = line.includes('{') ? 'diamond' : 'box';
      nodes.set(from, { label: fromLabel || from, shape: fromShape });
      if (toLabel || to) nodes.set(to, { label: toLabel || to, shape: 'box' });
      edges.push([from, to, label || undefined]);
    }
  }
  if (nodes.size === 0) return '(unparseable diagram)';
  const out: string[] = [];
  for (const [id, n] of nodes) {
    if (n.shape === 'diamond') out.push(`   ┌─${n.label}─┐`);
    else if (id === [...nodes.keys()][0]) out.push(`[${n.label}]`);
  }
  for (const [from, to, label] of edges) {
    out.push(`  │`);
    out.push(`  ▼  ${label || ''}`.trimEnd());
    out.push(`[${nodes.get(to)?.label || to}]`);
  }
  return out.join('\n');
}
```

**Step 1.4: Run test — confirm pass**
Command: same as 1.2
Expected: PASS (4/4)

**Step 1.5: Commit**
```bash
git add packages/coding-agent/src/util/mermaid-render.ts packages/coding-agent/test/unit/mermaid-render.test.ts
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(coding-agent/util): Mermaid → ASCII renderer 4-shape subset (D-30.5.1)"
```

---

## Task 2: MemoryEditor UI component (D-30.5.2)

**Files:**
- Create: `packages/tui-ink/src/components/MemoryEditor.tsx`
- Test: `packages/tui-ink/test/memoryeditor.test.tsx`

**Step 2.1: Write failing test**
```tsx
import { describe, it, expect } from 'vitest';
import React from 'react';
import { render } from 'ink-testing-library';
import { MemoryEditor } from '../src/components/MemoryEditor.js';

describe('MemoryEditor', () => {
  it('renders MEMORY.md content', () => {
    const { lastFrame } = render(
      <MemoryEditor content="user prefers Chinese" onSave={() => {}} />
    );
    expect(lastFrame()).toContain('MEMORY');
    expect(lastFrame()).toContain('user prefers Chinese');
  });
  it('shows save button hint', () => {
    const { lastFrame } = render(
      <MemoryEditor content="x" onSave={() => {}} />
    );
    expect(lastFrame()).toMatch(/save|ctrl/i);
  });
});
```

**Step 2.2: Run test — confirm fail**
Command: `cd packages/tui-ink && pnpm exec vitest run test/memoryeditor.test.tsx`
Expected: FAIL

**Step 2.3: Implement**
```tsx
// packages/tui-ink/src/components/MemoryEditor.tsx
import React from 'react';
import { Box, Text } from 'ink';

export interface MemoryEditorProps {
  content: string;
  onSave: (next: string) => void;
}

export const MemoryEditor: React.FC<MemoryEditorProps> = ({ content }) => (
  <Box flexDirection="column" borderStyle="round" paddingX={1}>
    <Text bold color="cyan">MEMORY.md</Text>
    <Text>{content}</Text>
    <Text dimColor>(press Ctrl+S to save · Esc to cancel)</Text>
  </Box>
);
```

**Step 2.4: Run test — confirm pass**
Command: same as 2.2
Expected: PASS

**Step 2.5: Commit**
```bash
git add packages/tui-ink/src/components/MemoryEditor.tsx packages/tui-ink/test/memoryeditor.test.tsx
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(tui-ink): MemoryEditor component (D-30.5.2)"
```

---

## Task 3: SkillLoader UI component (D-30.5.3)

**Files:**
- Create: `packages/tui-ink/src/components/SkillLoader.tsx`
- Test: `packages/tui-ink/test/skillloader.test.tsx`

**Step 3.1: Write failing test** (renders skill list, marks loaded)
```tsx
import { describe, it, expect } from 'vitest';
import React from 'react';
import { render } from 'ink-testing-library';
import { SkillLoader } from '../src/components/SkillLoader.js';

describe('SkillLoader', () => {
  it('lists skills with loaded marker', () => {
    const skills = [
      { name: 'writing-plans', loaded: true },
      { name: 'systematic-debugging', loaded: false },
    ];
    const { lastFrame } = render(<SkillLoader skills={skills} onToggle={() => {}} />);
    expect(lastFrame()).toContain('writing-plans');
    expect(lastFrame()).toContain('✓');
    expect(lastFrame()).toContain('systematic-debugging');
    expect(lastFrame()).toContain('○');
  });
});
```

**Step 3.2: Run test — confirm fail**
Expected: FAIL

**Step 3.3: Implement**
```tsx
// packages/tui-ink/src/components/SkillLoader.tsx
import React from 'react';
import { Box, Text } from 'ink';

export interface SkillLoaderProps {
  skills: ReadonlyArray<{ name: string; loaded: boolean }>;
  onToggle: (name: string) => void;
}

export const SkillLoader: React.FC<SkillLoaderProps> = ({ skills }) => (
  <Box flexDirection="column" borderStyle="round" paddingX={1}>
    <Text bold color="cyan">Skills</Text>
    {skills.map(s => (
      <Text key={s.name}>
        {s.loaded ? <Text color="green">✓ </Text> : <Text color="gray">○ </Text>}
        {s.name}
      </Text>
    ))}
  </Box>
);
```

**Step 3.4: Run test — confirm pass**
Expected: PASS

**Step 3.5: Commit**
```bash
git add packages/tui-ink/src/components/SkillLoader.tsx packages/tui-ink/test/skillloader.test.tsx
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(tui-ink): SkillLoader component (D-30.5.3)"
```

---

## Task 4: CronList UI component (D-30.5.4)

**Files:**
- Create: `packages/tui-ink/src/components/CronList.tsx`
- Test: `packages/tui-ink/test/cronlist.test.tsx`

**Step 4.1: Write failing test** (renders job list with enabled marker)
```tsx
import { describe, it, expect } from 'vitest';
import React from 'react';
import { render } from 'ink-testing-library';
import { CronList } from '../src/components/CronList.js';

describe('CronList', () => {
  it('renders cron jobs', () => {
    const jobs = [
      { id: 'j1', schedule: '0 9 * * *', prompt: 'morning brief', enabled: true },
      { id: 'j2', schedule: 'every 2h', prompt: 'watcher', enabled: false },
    ];
    const { lastFrame } = render(<CronList jobs={jobs} onToggle={() => {}} />);
    expect(lastFrame()).toContain('0 9 * * *');
    expect(lastFrame()).toContain('morning brief');
    expect(lastFrame()).toContain('●');
    expect(lastFrame()).toContain('○');
  });
});
```

**Step 4.2: Run test — confirm fail**
Expected: FAIL

**Step 4.3: Implement**
```tsx
// packages/tui-ink/src/components/CronList.tsx
import React from 'react';
import { Box, Text } from 'ink';

export interface CronJob {
  id: string;
  schedule: string;
  prompt: string;
  enabled: boolean;
}

export interface CronListProps {
  jobs: ReadonlyArray<CronJob>;
  onToggle: (id: string) => void;
}

export const CronList: React.FC<CronListProps> = ({ jobs }) => (
  <Box flexDirection="column" borderStyle="round" paddingX={1}>
    <Text bold color="cyan">Cron jobs</Text>
    {jobs.length === 0 && <Text dimColor>(no jobs)</Text>}
    {jobs.map(j => (
      <Text key={j.id}>
        {j.enabled ? <Text color="green">● </Text> : <Text color="gray">○ </Text>}
        <Text bold>{j.schedule.padEnd(12)}</Text> {j.prompt}
      </Text>
    ))}
  </Box>
);
```

**Step 4.4: Run test — confirm pass**
Expected: PASS

**Step 4.5: Commit**
```bash
git add packages/tui-ink/src/components/CronList.tsx packages/tui-ink/test/cronlist.test.tsx
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(tui-ink): CronList component (D-30.5.4)"
```

---

## Task 5: SessionList UI component (D-30.5.5)

**Files:**
- Create: `packages/tui-ink/src/components/SessionList.tsx`
- Test: `packages/tui-ink/test/sessionlist.test.tsx`

**Step 5.1: Write failing test** (renders session list with msg count)
```tsx
import { describe, it, expect } from 'vitest';
import React from 'react';
import { render } from 'ink-testing-library';
import { SessionList } from '../src/components/SessionList.js';

describe('SessionList', () => {
  it('renders session rows', () => {
    const sessions = [
      { id: 'abc12345', firstUser: 'fix auth bug', messageCount: 12, createdAt: Date.now() },
    ];
    const { lastFrame } = render(
      <SessionList sessions={sessions} onLoad={() => {}} />
    );
    expect(lastFrame()).toContain('abc12345');
    expect(lastFrame()).toContain('fix auth bug');
    expect(lastFrame()).toContain('12');
  });
});
```

**Step 5.2: Run test — confirm fail**
Expected: FAIL

**Step 5.3: Implement**
```tsx
// packages/tui-ink/src/components/SessionList.tsx
import React from 'react';
import { Box, Text } from 'ink';

export interface SessionRow {
  id: string;
  firstUser: string;
  messageCount: number;
  createdAt: number;
}

export interface SessionListProps {
  sessions: ReadonlyArray<SessionRow>;
  onLoad: (id: string) => void;
}

export const SessionList: React.FC<SessionListProps> = ({ sessions }) => (
  <Box flexDirection="column" borderStyle="round" paddingX={1}>
    <Text bold color="cyan">Sessions</Text>
    {sessions.length === 0 && <Text dimColor>(no sessions)</Text>}
    {sessions.map(s => (
      <Text key={s.id}>
        <Text color="yellow">{s.id.slice(0, 8)}</Text>
        {' '}<Text>{s.firstUser.slice(0, 40)}</Text>
        {' '}<Text dimColor>({s.messageCount} msg)</Text>
      </Text>
    ))}
  </Box>
);
```

**Step 5.4: Run test — confirm pass**
Expected: PASS

**Step 5.5: Commit**
```bash
git add packages/tui-ink/src/components/SessionList.tsx packages/tui-ink/test/sessionlist.test.tsx
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(tui-ink): SessionList component (D-30.5.5)"
```

---

## Task 6: CodeReviewCard UI component (D-30.5.6)

**Files:**
- Create: `packages/tui-ink/src/components/CodeReviewCard.tsx`
- Test: `packages/tui-ink/test/codereviewcard.test.tsx`

**Step 6.1: Write failing test** (renders review verdict + issues)
```tsx
import { describe, it, expect } from 'vitest';
import React from 'react';
import { render } from 'ink-testing-library';
import { CodeReviewCard } from '../src/components/CodeReviewCard.js';

describe('CodeReviewCard', () => {
  it('renders review verdict + issues', () => {
    const { lastFrame } = render(
      <CodeReviewCard
        verdict="block"
        issues={[
          { severity: 'block', file: 'src/x.ts', line: 42, message: 'unsafe' },
        ]}
        onAck={() => {}}
      />
    );
    expect(lastFrame()).toContain('block');
    expect(lastFrame()).toContain('src/x.ts:42');
    expect(lastFrame()).toContain('unsafe');
  });
});
```

**Step 6.2: Run test — confirm fail**
Expected: FAIL

**Step 6.3: Implement**
```tsx
// packages/tui-ink/src/components/CodeReviewCard.tsx
import React from 'react';
import { Box, Text } from 'ink';

export interface ReviewIssue {
  severity: 'block' | 'nit' | 'suggestion';
  file: string;
  line: number;
  message: string;
}

export interface CodeReviewCardProps {
  verdict: 'block' | 'approve' | 'comment';
  issues: ReadonlyArray<ReviewIssue>;
  onAck: () => void;
}

const colorFor = (s: ReviewIssue['severity']): 'red' | 'yellow' | 'gray' =>
  s === 'block' ? 'red' : s === 'nit' ? 'yellow' : 'gray';

export const CodeReviewCard: React.FC<CodeReviewCardProps> = ({ verdict, issues }) => (
  <Box flexDirection="column" borderStyle="round" paddingX={1}>
    <Text bold>Code Review — <Text color={verdict === 'block' ? 'red' : 'green'}>{verdict}</Text></Text>
    {issues.map((i, idx) => (
      <Text key={idx}>
        <Text color={colorFor(i.severity)}>[{i.severity}]</Text>{' '}
        {i.file}:{i.line} — {i.message}
      </Text>
    ))}
  </Box>
);
```

**Step 6.4: Run test — confirm pass**
Expected: PASS

**Step 6.5: Commit**
```bash
git add packages/tui-ink/src/components/CodeReviewCard.tsx packages/tui-ink/test/codereviewcard.test.tsx
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(tui-ink): CodeReviewCard component (D-30.5.6)"
```

---

## Task 7: requesting-code-review skill (D-30.5.7)

**Files:**
- Create: `packages/coding-agent/src/skills/requesting-code-review.ts`
- Test: `packages/coding-agent/test/unit/requesting-code-review.test.ts`

**Step 7.1: Write failing test** (engine module: scan for `## Review Checklist` markers)
```ts
import { describe, it, expect } from 'vitest';
import { reviewChecklist } from '../../src/skills/requesting-code-review.js';

describe('requesting-code-review', () => {
  it('returns review checklist items', () => {
    const items = reviewChecklist();
    expect(items.length).toBeGreaterThan(0);
    expect(items.some(i => i.toLowerCase().includes('security'))).toBe(true);
  });
  it('flags blocking categories', () => {
    const flags = reviewChecklist({ category: 'block' });
    expect(flags.length).toBeGreaterThan(0);
  });
});
```

**Step 7.2: Run test — confirm fail**
Expected: FAIL

**Step 7.3: Implement** (in-code skill, not Hermes-skill format)
```ts
// packages/coding-agent/src/skills/requesting-code-review.ts
/**
 * Requesting Code Review skill (D-30.5.7, 2026-06-08).
 *
 * Engine module form (跟 ~/.hermes/skills/ 形式区别). 集成进 coding-agent
 * 的 pre-commit hook: 跑 reviewChecklist() 给出必查项, LLM-driven reviewer
 * 拿这 list 扫 diff.
 *
 * 红线: 不直接调 LLM, 不接 network, 不存 state — 纯函数 return list.
 */
export interface ReviewChecklistOptions {
  category?: 'block' | 'nit' | 'all';
}

const BLOCK: ReadonlyArray<string> = [
  'Security: secrets in code (API keys, tokens, passwords)',
  'Security: SQL/XSS/command injection vectors',
  'Correctness: 5 红线 preserved (D-19.5 P2-SIGINT, D-19.6.1, 6afccc8, 1ceef94, no-unsafe-finally)',
  'Correctness: turnInFlight/lineQueue state machine unchanged',
  'Public API: 1:1 shape preserved (no breaking renames)',
];

const NIT: ReadonlyArray<string> = [
  'Style: 0 dead code, 0 commented-out blocks',
  'Tests: new code has failing test → green test',
  'Docs: README + plan + ship log updated if public surface changed',
  'Commit: subject under 72 chars, body explains WHY not WHAT',
];

export function reviewChecklist(opts: ReviewChecklistOptions = {}): string[] {
  const cat = opts.category ?? 'all';
  if (cat === 'block') return [...BLOCK];
  if (cat === 'nit') return [...NIT];
  return [...BLOCK, ...NIT];
}
```

**Step 7.4: Run test — confirm pass**
Expected: PASS

**Step 7.5: Commit**
```bash
git add packages/coding-agent/src/skills/requesting-code-review.ts packages/coding-agent/test/unit/requesting-code-review.test.ts
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(coding-agent): requesting-code-review skill engine (D-30.5.7)"
```

---

## Task 8: Update /help text to list 14 slash commands (D-30.5.8)

**Files:**
- Modify: `packages/coding-agent/src/repl/repl-command-router.ts` (help case in `dispatchSlashBuiltin`)

**Step 8.1: Verify current help text is missing 5 commands**
Read the help text (already done in D-30.5 recon). Currently lists 9 (help, clear, new, status, verify, theme, model, tools, exit). Missing: /memory, /skills, /cron, /sessions, /load, /plan.

**Step 8.2: Add 5 missing commands to help text** (keep exit at bottom, group by category)
```ts
const help = [
  'Available slash commands:',
  '',
  '  Session:',
  '    /new         start a new session',
  '    /sessions    list past sessions',
  '    /load <id>   resume a session',
  '',
  '  State:',
  '    /status      show model/session/ema',
  '    /theme [n]   switch theme (default/solarized/monochrome)',
  '    /model [id]  switch LLM model',
  '    /tools       list registered tools',
  '',
  '  Knowledge:',
  '    /memory      view MEMORY.md',
  '    /skills      list loaded skills',
  '    /cron        list cron jobs',
  '',
  '  Mode:',
  '    /plan        enter plan mode',
  '    /verify      run verify checks',
  '    /clear       clear the screen',
  '    /help        show this help',
  '',
  '  Exit:',
  '    /exit, /q, /quit   exit REPL',
].join('\n');
```

**Step 8.3: Run slash command tests to confirm 0 业务 改业务**
```bash
cd packages/coding-agent && pnpm exec vitest run test/unit/repl-command-router.test.ts
```
Expected: PASS (no behavior change, only help text formatting)

**Step 8.4: Commit**
```bash
git add packages/coding-agent/src/repl/repl-command-router.ts
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "feat(coding-agent/repl): /help 列出 14 命令按类别 (D-30.5.8)"
```

---

## Task 9: Bump 5 packages 1.0.14 → 1.0.15 + ship marker (D-30.5.9)

**Step 9.1: Update package.json versions**
```bash
for pkg in core edit-engine llm coding-agent tui-ink; do
  sed -i 's/"version": "1.0.14"/"version": "1.0.15"/' packages/$pkg/package.json
done
sed -i 's/1.0.14/1.0.15/g' README.md
```

**Step 9.2: Run pnpm test (HARD GATE)**
```bash
cd /d/App/openClaw/projects/deepwhale && pnpm test 2>&1 | tail -5
```
Expected: ~791 + ~9 new (6 UI tests + 1 mermaid + 1 code-review + 1 help) = **800+** pass / 5 skip / 0 fail

**Step 9.3: Commit bump**
```bash
git add packages/*/package.json README.md
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit -m "bump: 5 packages 1.0.14 → 1.0.15 + README sync (D-30.5.9)"
```

**Step 9.4: Ship marker (empty commit)**
```bash
git -c user.name=butterfly443 -c user.email=butterfly443@openclaw.ai commit --allow-empty -m "ship(coding-agent): D-30.5 全收口 (8 commit, 791→800+ pass, 5 红线 0 改, 1.0.15 ready, D-30 核心 plan 100% close)"
```

---

## Task 10: Force-push release/v1.0 + tag v1.0.15 (D-30.5.10)

```bash
git push origin release/v1.0 --force-with-lease
git tag -a v1.0.15 -m "D-30.5 收口 8 commit: Mermaid + 5 UI + 1 skill + help text + 1.0.15"
git push origin v1.0.15
```

---

## Acceptance Criteria

- 9 feature commits + 1 ship marker = 10 commit
- pnpm test **800+ pass / 5 skip / 0 fail**
- 1 Mermaid 渲染 + 5 UI 组件 (MemoryEditor / SkillLoader / CronList / SessionList / CodeReviewCard) + 1 skill (requesting-code-review) + /help 14 命令列全
- 5 packages v1.0.15
- 0 业务改业务, 0 破 5 红线
- release/v1.0 force-pushed, tag v1.0.15 on origin

## Out of Scope (留 D-31+)

- 5 packages publish to npm (需 user token)
- DiagramRenderer 升级 (Mermaid 完整语法, 现在 4-shape subset)
- Code review skill 集成 pre-commit hook (现仅 return list)

## 5 红线 preserved

| 红线 | 文件 | 验法 |
|------|------|------|
| D-19.5 P2-SIGINT | repl.ts L370-373 + L409-418 | grep `SIGINT` 不在 diff |
| D-19.6.1 turnInFlight guard | repl.ts L409-418 | grep `turnInFlight` 不在 diff |
| 6afccc8 deny 非 /exit /quit | repl.ts L412 | grep `turnInFlight.*deny` 不在 diff |
| 1ceef94 /verify try/finally | verify/index.ts | grep `try/finally\|appendVerificationEvent` 不在 diff |
| no-unsafe-finally | verify-runner L147 | grep `unsafe-finally` 不在 diff |
