# D-29.2 Self-Review Report v3 — SHIPPED, reviewer 措辞校正 follow-up

Sprint: 1c-revive-5-D-29.2 (2026-06-07, D-29 sub-plan #2/3)
执行者: Hermes (subagent 派单 + 我实跑 verify + 候选 1 拍板实施 + reviewer 措辞校正)
状态: **SHIPPED 2026-06-07 12:35, 2 commit 已 push 远端 (b149210 + 80cf461)**
日期: 2026-06-07
前置: v1 self-review 5932 bytes (b149210 内), v2 self-review 5302 bytes (80cf461 内)

## 1. Ship 状态 (确认)

- commit b149210: `fix(policy): sanitizeReason 加 3 类 secret redact (D-29.2, 候选 1 拍板)`
  - 3 files, +244/-12
  - 已 push 远端 (5f0bfc9..b149210)
- commit 80cf461: `docs(plans): D-29.2 self-review v2 — ship blocked status report`
  - 1 file, +103/-0
  - 已 push 远端 (b149210..80cf461)
- 远端真验证: `git ls-remote origin main` = 80cf461 ✓ (跟本机一致, 0 偏差)
- v1.0.12 (aaaa8ad) + closure (5f0bfc9) + D-29.2 ship (b149210) + v2 doc (80cf461) = 4 commit cluster ship

## 2. Reviewer 措辞校正 (3 follow-up, 本 v3 实施)

### Follow-up 1 (重要 — 措辞校正) — 接受 + 实施

**Reviewer 发现**: 我 commit message / v1 doc 措辞 "极长 800 字符 JWT 才漏" 跟实测不符。

**实测** (reviewer 写 /tmp/test-leak.mjs 跑):
```
Input:  'a' * 180 + ' sk-abcdefghij1234567 ' + 'b' * 50  (252 chars)
Output: 'a' * 180 + ' sk-abcd…[truncated]'              (200 chars)
```
- 不是 "极长 800 字符 JWT 才漏"
- **任何长度 secret 撞在 200 字符边界附近都会被切** (20 字符 sk- 都漏 4 字符)

**实施 (本 v3)**:
- 改源 sanitize-reason.ts L51-57 注释: 措辞 "极长 secret (如 800 字符 JWT) 跨 truncate 边界漏 redact 边缘" → "任何 secret 撞 200 字符边界" + 实测 252 字符 + 20 字符 sk- 漏 4 字符
- 改测 L83-104 注释: 同校正, 加 "r2 实际: sk-abcd 4 字符子串 (候选 1 拍板接受)" + 防止 future reviewer 加 not.toMatch(/sk-/) 强约束致 fail
- 4 红线实跑: 14/14 绿, typecheck 0, lint 0 0 warning

### Follow-up 2 (小 — v2 标题 "ship blocked" 矛盾) — 接受 + 实施

**Reviewer 发现**: v2 doc 标题 "ship blocked status report" 跟实际 ship 矛盾 (已 push 80cf461)。

**实施 (本 v3)**:
- 本 v3 doc 替代 v2 标题 (本 doc 标题 "SHIPPED, reviewer 措辞校正 follow-up")
- v2 doc 80cf461 内保留 (有 audit 价值, 含 ship 决策上下文), 不重写
- 本 v3 doc 落 follow-up commit, 标 "ship 状态确认 + reviewer 校正"

### Follow-up 3 (小 — 测 L83-104 注释防误强约束) — 接受 + 实施 (含在 follow-up 1)

**Reviewer 发现**: 测 B 没断言 "no sk- substring leak", 未来 reviewer 加 not.toMatch(/sk-/) 会 fail。

**实施 (本 v3, 跟 follow-up 1 一起改)**:
- 测 L83-104 注释加: "防止 future reviewer 加 not.toMatch(/sk-/) 强约束致候选 1 实现 fail, 看本注释知拍板已明示接受"

## 3. 4 红线实测 (我亲自跑, 不信 subagent self-report)

| 红线 | 命令 | 结果 |
|---|---|---|
| typecheck 0 错 | `pnpm typecheck` | ✅ exit 0 |
| lint 0 error 0 warning | `pnpm lint` | ✅ exit 0 |
| sanitize-reason.test.ts 14/14 | `pnpm exec vitest run packages/coding-agent/test/policy/sanitize-reason.test.ts` | ✅ 14/14 绿 |
| 全 coding-agent 305+ passed | `pnpm exec vitest run packages/coding-agent` | ✅ 392 passed / 11 skipped / 0 fail (Linux baseline) |

注: Reviewer 报 Windows git-bash + zh_CN 下 5 pre-existing fail (REPL turn-guard deny / verify-runner spawn-error / TUI smoke 0o600 / TUI smoke builtin filter / REPL /verify i18n), D-29.2 之前 (5f0bfc9) 同样 fail, 跟 D-29.2 无关, 平台 issue 不在 D-29.2 范围。

## 4. 5 红线 grep 验保留 (D-29.2 plan §3 红线 5 条 0 漂移)

| 红线 | grep 命令 | 命中 |
|---|---|---|
| 1. D-13 MVP 长度截断+折叠+去 NUL 保留 | `grep -nE 'MAX_REASON_LEN\|\\r?\\n\|\\u0000' sanitize-reason.ts` | ✅ L19 / L61-62 / L67 / L70 |
| 2. tool-loop.ts 5 处调用 signature 不变 | `grep -nE 'sanitizeReason' tool-loop.ts \| wc -l` | ✅ 6 处 (1 import + 5 调用) |
| 3. sanitize-reason 不引新 import, 0 新 dep | `head -5 sanitize-reason.ts` | ✅ 0 import 行 |
| 4. 14 it 测覆盖 | `grep -cE '^  it\(' sanitize-reason.test.ts` | ✅ 14 |
| 5. 0 改 sanitizeReason 签名 | `grep -nE 'export function sanitizeReason'` | ✅ L58 单点 |

## 5. 候选 1 拍板边缘 case (reviewer 校正后)

**实测 leak 场景** (reviewer 写 /tmp/test-leak.mjs 跑):
- Input 252 字符 (180 a + 空格 + 20 字符 sk- + 空格 + 50 b)
- Output 200 字符 (180 a + 空格 + 'sk-abcd' 4 字符 + '…[truncated]' 13 字符)
- 漏 redact 4 字符: `sk-abcd`

**200 字符窗口覆盖**:
- OpenAI sk- 形式: 51 字符
- GitHub PAT: 40 字符
- AWS access key: 20 字符
- AWS secret: 40 字符
- DeepSeek: 35 字符
- Anthropic: 108 字符

**漏 redact 撞边界场景**:
- 嵌在长 reason (e.g. 长 error message 含内嵌 token) 的中后段
- 不限于 "极长 800 字符 JWT", 任何长度 secret 撞边界都漏
- 拍板接受: trade-off 选最简单, 牺牲撞边界子串, 4 字符子串在审计场景下风险低

**future reviewer 加 not.toMatch(/sk-/) 强约束会 fail**:
- 测 L83-104 注释明示接受, 防止误强约束

## 6. Superpowers 流程 (完整审计)

| 步 | 状态 |
|---|---|
| 1. subagent 派单含 self-review 要求 | ✅ 派单 5 步指令 |
| 2. 我实跑 verify (不信 subagent self-report) | ✅ 4 红线 + 5 grep, 抓到 v1 1 fail (truncate 切 marker) |
| 3. 写 self-review report md + 飞书摘要 | ✅ v1 (5932 bytes, b149210) + v2 (5302 bytes, 80cf461) + v3 (本 doc, follow-up commit) |
| 4. **等 user 拍 ship / 改 / 返工** | ✅ user 拍 ship (D-29.2 b149210 ship 完整) |
| 5. push + 远端真验证 + 飞书推送通知 | ✅ 5f0bfc9..b149210 push, b149210..80cf461 push, git ls-remote 验 80cf461 |
| 6. **reviewer 措辞校正 follow-up** (本 v3) | ⏳ 3 follow-up 实施完, 等 user 拍 ship follow-up commit |

## 7. 文件状态 (follow-up commit 实施后, 0 push)

```
M packages/coding-agent/src/policy/sanitize-reason.ts       (措辞校正, 0 改逻辑)
M packages/coding-agent/test/policy/sanitize-reason.test.ts  (措辞校正 + 防误强约束注释, 0 改逻辑)
?? docs/plans/2026-06-07-D-29.2-self-review-v3.md           (本 doc, 落 follow-up commit)
```

(注: 0 改 .hermes/plans/ 用户 3 untracked)

## 8. 拍板请求 (user)

**follow-up commit ready**. 3 修:
- 源注释 L51-57 措辞校正 (实测 252 字符 + 20 字符 sk- → 漏 4 字符)
- 测注释 L83-104 措辞校正 + 防误强约束
- v3 doc 落 follow-up commit

请拍:
- **ship** → 我跑 `git add + commit + push origin main` + 飞书推送通知
- **改** → 指出哪改, 我重做
- **返工** → 指出问题, 我返工

superpowers 红线: **没 user 拍 ship, 不 push**.

## 9. Ship 后 (如果 user 拍 ship) — D-29.3 入口

- push follow-up commit 远端 + 飞书推送通知
- 跑远端真验证 (git ls-remote origin main)
- 准备 D-29.3 派单 (按你 checklist: cat plan + 派 subagent 加 4 组件 ink-testing-library 测)
- D-29.3 ready 后同样 superpowers 5 步循环
