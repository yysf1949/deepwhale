# D-29.3 startRepl Internal Extract — Implementation Plan

> **For implementer:** Refactor 抽 startRepl 内 4 段 (finish / line handler / close
> handler / preamble) + 1 trim commit, 5 commit 累计. **TDD 不适用** — 行为 1:1
> 保 628 既有测试, **不**写新测试.
>
> **本文件**只覆盖 D-29.3 (startRepl 内部切片), repl.ts 515L → 目标 ≤300L
> (D-29.1.4 + D-29.2 + D-29.3 累计 -65% from 947L).

**Goal:** 把 `packages/coding-agent/src/repl.ts` 515L 内 `startRepl` 函数的 4 段
内部职责 (`finish` 22L + `rl.on('line')` 117L + `rl.on('close')` 27L + preamble
85L = **251L**) 抽到 `src/repl/*.ts` 子目录, repl.ts 缩到 ≤300L, 5 红线 0 改 (行为
1:1 保), 公共 API 0 改, 628 既有测试 0 new fail.

**Architecture:** **state 共享 + factory 模式** — `startRepl` 内部定义 mutable
`ReplState` 对象 (turnInFlight / pendingExit / exitTimer / lineQueue / exiting
5 字段), 抽出的 4 个 handler 工厂 (`createFinish` / `createLineHandler` /
`createCloseHandler` / `createReplBootstrap`) 收 state + 依赖, 返回闭包函数
或 `void`. 闭包持有 state 引用, 跨 handler 共享 mutable state — 跟现
`repl-confirm.ts` / `repl-signal-coordinator.ts` 工厂模式同形态, 不引新 dep.

**Tech Stack:** TypeScript 5.7 (strict), Node 22 内置, 现有 LLMClient /
CompactionState / ToolPolicy / SessionWriter / RLInterface.

---

## §1 现状 (release/v1.0 HEAD c4d5733)

`packages/coding-agent/src/repl.ts` 515L, `startRepl` 函数 (L139-510) 占 **372L**:

| 段 | 行号 | 行数 | 职责 | 红线 |
| --- | --- | --- | --- | --- |
| Preamble | L139-276 | 137 | 解析 options + 懒 client + session + compaction + rl | — |
| `finish()` | L287-309 | 22 | REPL 收尾 (dispose + close writer + resolve) | D-19.5 P2-SIGINT |
| State decls | L311-340 | 30 | turnInFlight / lineQueue / exitTimer / signalCoordinator | 6afccc8 P1 + D-19.6 P1 |
| `rl.on('line')` | L355-471 | 117 | 主 line handler (confirm / slash / chat / queue / finally) | 6afccc8 P1 + 1ceef94 + no-unsafe-finally + D-19.6.1 |
| `rl.on('close')` | L474-500 | 27 | stdin EOF (dismiss + abort + exitTimer 30s) | D-19.5 P2-dismiss + D-19.6 P1 |
| `prompt` | L502-509 | 8 | 第一个 prompt + 重 prompt | — |

**5 红线保留** = 抽 4 段时**不**改**行为**, 只挪代码位置. 跑过 `git diff
c4d5733..release/v1.0` 验 5 红线**行为** 1:1 (允许行号浮动, 业务逻辑 0 改):
- D-19 P1/P2 (finish + SIGINT listener) — 抽 `finish` 时**严格**保 dispose 顺序
- 6afccc8 P1 (turnInFlight / lineQueue / pendingExit state machine) — 抽 line handler 时 6 段**逐字**保
- 1ceef94 P1 (/verify try/finally) — 已走 command-router, 不动
- D-19.6.1 P2.1 (catch 分支 signal.aborted 优先) — 已走 agent-turn, 不动
- no-unsafe-finally (finally 块 if/else if/else 链) — 抽 line handler 时**严格**保

**公共 API 0 改**: `startRepl(options: ReplOptions): Promise<number>` 签名 1:1 保.

---

## §2 D-29.3.1 抽 `finish()` (Commit 1)

### §2.1 抽什么 (L280-309, 30L 含注释)

`finish` 闭包 (L287-309) — REPL 收尾入口, 处理:
1. `exiting` 守卫幂等
2. `exitTimer` 清理
3. `signalCoordinator.dispose()` (D-19.5 P2-SIGINT 顺序: dispose 必须在 rl.close 之前)
4. `rl.close()`
5. `writer.close()` (best-effort)
6. `out.write(goodbye)`
7. `resolve(code)`

### §2.2 抽后接口 (repl-finish.ts)

```ts
export interface ReplFinishState {
  exiting: boolean;
  exitTimer: NodeJS.Timeout | null;
}

export interface ReplFinishDeps {
  state: ReplFinishState;
  signalCoordinator: ReplSignalCoordinator;
  rl: RLInterface;
  writer: SessionWriter | null;
  out: NodeJS.WritableStream;
  t: typeof import('@deepwhale/core').t;
  resolve: (code: number) => void;
}

export function createFinish(deps: ReplFinishDeps): (code: number) => Promise<void>;
```

### §2.3 抽后 repl.ts 改法 (2 处)

1. 加 import: `import { createFinish } from './repl/repl-finish.js';`
2. 删 L280-309 整段, `finish` 改从 `createFinish({...})` 来

### §2.4 严格红线 (0 改)

- **0 改业务逻辑** — finish 函数体 1:1, dispose / close / resolve 顺序**逐字**保
- **0 改公共 API** — startRepl 签名 0 改
- **0 改 5 红线** — D-19.5 P2-SIGINT dispose 顺序保

### §2.5 验证

1. `pnpm test` → 0 new fail (vs c4d5733 baseline 628/5/0)
2. **不 push, 不 tag** — 5 commit 攒齐再 push

---

## §3 D-29.3.2 抽 `rl.on('line')` handler (Commit 2)

### §3.1 抽什么 (L355-471, 117L)

主 line handler, 6 段:
1. confirmController.hasPending() → offerLine 串行化 (D-19)
2. turnInFlight slash builtin guard (D-19.6.1 + 6afccc8)
3. empty line / exit /quit fast-path (D-19.5 P1)
4. dispatchSlashBuiltin (/help /verify /unknown, D-29.1.3)
5. turnInFlight → lineQueue 排队 (D-19.5 P1)
6. chat turn (runAgentTurn) + finally drain (no-unsafe-finally + D-19.6.1)

### §3.2 抽后接口 (repl-line-handler.ts)

```ts
export interface ReplLineState {
  turnInFlight: boolean;
  pendingExit: boolean;
  lineQueue: string[];
  exiting: boolean;
}

export interface ReplLineDeps {
  state: ReplLineState;
  finish: (code: number) => Promise<void>;
  signalCoordinator: ReplSignalCoordinator;
  confirmController: ReplConfirmController;
  tryCreateClient: () => { client: LLMClient | null; error: Error | null };
  clientFromOptions: LLMClient | null;
  runAgentTurn: typeof import('./repl-agent-turn.js').runAgentTurn;
  dispatchSlashBuiltin: typeof import('./repl-command-router.js').dispatchSlashBuiltin;
  // 透传
  out: NodeJS.WritableStream;
  err: NodeJS.WritableStream;
  writer: SessionWriter | null;
  workingMessages: ChatMessage[];
  emaState: UsageEmaState;
  compactionConfig: AgentCompactionConfig | null;
  sandboxRunner: SandboxRunner | undefined;
  policyYes: boolean;
  replPolicy: ToolPolicy;
  enableToolLoop: boolean;
  verifyChecks: VerifyCheck[] | undefined;
  t: typeof import('@deepwhale/core').t;
  rl: RLInterface;  // for setImmediate(() => rl.emit('line', next))
}

export function createLineHandler(deps: ReplLineDeps): (rawLine: string) => Promise<void>;
```

### §3.3 抽后 repl.ts 改法 (2 处)

1. 加 import: `import { createLineHandler } from './repl/repl-line-handler.js';`
2. 删 L355-471 整段, `rl.on('line', ...)` 改用 `createLineHandler({...})`

### §3.4 严格红线 (0 改)

- **0 改业务逻辑** — line handler 6 段 1:1, 顺序 1:1
- **0 改 5 红线**:
  - 6afccc8 P1 (turnInFlight / lineQueue / pendingExit) 1:1
  - D-19.6.1 P2.1 (signal.aborted 优先) — 走 runAgentTurn 内部, 不动
  - no-unsafe-finally (finally 块 if/else if/else 链) 1:1

### §3.5 验证

1. `pnpm test` → 0 new fail

---

## §4 D-29.3.3 抽 `rl.on('close')` handler (Commit 3)

### §4.1 抽什么 (L474-500, 27L)

close handler: stdin EOF (管道/Ctrl-D) → 优雅退出
1. confirmController.dismiss() (D-19.5 P2-dismiss 顺序)
2. signalCoordinator.abortIfActive() (D-19.6 P1)
3. pendingExit = true (finally 兜底)
4. exitTimer 30s 兜底 (D-19.6 P1)

### §4.2 抽后接口 (repl-close-handler.ts)

```ts
export interface ReplCloseState {
  turnInFlight: boolean;
  pendingExit: boolean;
  exitTimer: NodeJS.Timeout | null;
}

export interface ReplCloseDeps {
  state: ReplCloseState;
  signalCoordinator: ReplSignalCoordinator;
  confirmController: ReplConfirmController;
  finish: (code: number) => Promise<void>;
  err: NodeJS.WritableStream;
  t: typeof import('@deepwhale/core').t;
}

export function createCloseHandler(deps: ReplCloseDeps): () => void;
```

### §4.3 抽后 repl.ts 改法 (2 处)

1. 加 import: `import { createCloseHandler } from './repl/repl-close-handler.js';`
2. 删 L474-500 整段, `rl.on('close', ...)` 改用 `createCloseHandler({...})`

### §4.4 严格红线 (0 改)

- D-19.5 P2-dismiss 顺序保
- D-19.6 P1 30s exitTimer 兜底保

---

## §5 D-29.3.4 抽 preamble / bootstrap (Commit 4)

### §5.1 抽什么 (L140-269, 130L)

- L142-189 (48L): lazy client init / tryCreateClient
- L190-203 (14L): sandboxRunner / policyYes / enableToolLoop / sessionPath
- L204-216 (13L): confirmController / replPolicy
- L218-230 (13L): greeting + api_key_missing warning
- L232-252 (21L): session loading (writer / reader / loadSession)
- L254-269 (16L): compaction config 构造

### §5.2 抽后接口 (repl-bootstrap.ts)

```ts
export interface ReplBootstrapResult {
  clientFromOptions: LLMClient | null;
  tryCreateClient: () => { client: LLMClient | null; error: Error | null };
  sandboxRunner: SandboxRunner | undefined;
  policyYes: boolean;
  enableToolLoop: boolean;
  sessionPath: string | undefined;
  confirmController: ReplConfirmController;
  replPolicy: ToolPolicy;
  writer: SessionWriter | null;
  workingMessages: ChatMessage[];
  emaState: UsageEmaState;
  compactionConfig: AgentCompactionConfig | null;
}

export interface ReplBootstrapDeps {
  options: ReplOptions;
  out: NodeJS.WritableStream;
  err: NodeJS.WritableStream;
  t: typeof import('@deepwhale/core').t;
  version: string;
}

export async function createReplBootstrap(deps: ReplBootstrapDeps): Promise<ReplBootstrapResult>;
```

### §5.3 抽后 repl.ts 改法 (2 处)

1. 加 import: `import { createReplBootstrap } from './repl/repl-bootstrap.js';`
2. 删 L140-269 (除 `out`/`err` setup), 改用 `await createReplBootstrap({...})`

### §5.4 严格红线 (0 改)

- **0 改业务逻辑** — bootstrap 行为 1:1
- **0 改公共 API** — startRepl 签名 0 改
- D-11-4 (lazy client), D-6 (compaction), D-13 (--yes), D-15 (confirm) 行为保

---

## §6 D-29.3.5 trim 收口 (Commit 5)

### §6.1 抽什么

- **无新模块抽** — 跟 D-29.2.3 形态一致
- 跟新 startRepl 顶部 docstring 提及 12 个子模块 (新增 4)
- 跟新底部 D-29.3 marker

### §6.2 严格红线 (0 改)

- 0 改业务, 0 改 5 红线

### §6.3 验证

1. `pnpm test` → 0 new fail
2. `git log -5 --oneline` 验 5 commit 顺序
3. `git push origin release/v1.0` → 远端 5 commit 新增
4. **不 tag, 不 rebase**

---

## §7 命名约定 (跟现有 repl/*.ts 对齐)

- 文件: `repl-<name>.ts` (kebab-case)
- 接口: `Repl<Name>State` / `Repl<Name>Deps` (state 是 mutable 共享, deps 是工厂入参)
- 工厂: `create<Name>(deps): <return type>` (跟 `createReplConfirm` / `createSignalCoordinator` 同形态)

---

## §8 后续 sub-cmd 留给 D-29.4+ (本 PR 不做)

| Sub-cmd | 抽什么 | 文件 | 留待 |
| --- | --- | --- | --- |
| D-29.4.1 | ReplOptions interface 抽 | `repl/repl-options.ts` | 后续 |
| D-29.4.2 | `prompt` 抽 | `repl/repl-prompt.ts` | 后续 |
| D-29.4.3 | startRepl 顶层 orchestrator 抽 | `repl/repl-orchestrator.ts` | 后续 |

D-29.3 收口后, repl.ts 估计 ≤300L. 进一步抽需要拆 startRepl 顶层, 风险大 (公共
API 边界), 留给 D-29.4+.

---

## §9 验证总表 (5 commit 收口时跑)

| 检查项 | 命令 | 期望 |
| --- | --- | --- |
| 5 红线 1:1 | `git diff c4d5733..release/v1.0` | 5 段红线**行为** 1:1 (允许代码位置浮动) |
| 测试 0 new fail | `pnpm test` | 628 pass / 5 skip / 0 fail |
| 行数缩 | `wc -l packages/coding-agent/src/repl.ts` | 515L → ≤300L |
| 模块文件数 | `ls packages/coding-agent/src/repl/*.ts` | 8 → 12 文件 (新增 4) |
| 公共 API | `git diff c4d5733..release/v1.0 -- src/index.ts` | re-export 列表 1:1 |
| 5 commit 顺序 | `git log -5 --oneline` | 3.1 → 3.2 → 3.3 → 3.4 → 3.5 |
| 推送 | `git push origin release/v1.0` | exit 0, 远端 5 commit 新增 |

---

## §10 Risks (拍板已知)

- **R-1**: D-29.3.2 抽 line handler 跨 ~10 个 dep, 接口 `ReplLineDeps` 字段多. 风
  险: 字段漏传 → line handler 跑时 undefined 抛错. 缓解: 每 commit 跑
  `pnpm test` 验 628/5/0, 漏一个就 0 跑通.
- **R-2**: D-29.3.4 抽 bootstrap 改 tryCreateClient / session loading / compaction
  3 段耦合, 失败点最多. 缓解: 抽之前先把 3 段**完整**读一遍, 接口收全
  (out / err / t / version / options 5 dep); 1 commit 干完, 跑通再进 3.5.
- **R-3**: 抽 state 共享 (mutable object) 闭包图复杂, 不如直接传值类型. 缓解:
  ReplState 字段全放最简 (boolean / 数组 / 引用), 不引复杂类型; handlers 共享
  同一 state 引用, 行为保.

---

## §11 关联 sprint

- **D-29.1.1 ~ 1.4** (2026-06-07): signal-coord / session / router / trim (947L → 754L)
- **D-29.2** (2026-06-07): utilities cluster (754L → 515L)
- **D-29.3** (本 PR): startRepl 内部切片 (515L → ≤300L)
- **D-29.4+** (后续): startRepl 顶层 + ReplOptions 抽 (≤300L → ≤200L?)

---

## §12 Execution Modes

**本 PR 执行模式:** **直接执行 (manual)** — 跟 D-29.2 形态一致. 抽法形态已被
D-29.1 + D-29.2 锁死, 重复 subagent 没收益. 每个 commit 流程:
1. 写新文件 (机械迁移, 1:1 行为)
2. 改 repl.ts (删函数体, 加 import, 调 createXxx({...}))
3. `pnpm test` 跑过
4. `git add` + `git commit -m "..."`
5. **不 push, 不 tag** — 5 commit 攒齐再 push

5 commit 攒齐后, 跑 §9 验证总表, push 到 `release/v1.0`. 跟 D-29.2 ship ritual
形态一致.
