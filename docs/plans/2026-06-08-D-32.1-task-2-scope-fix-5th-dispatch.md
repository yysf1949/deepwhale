# D-32.1 Task 2 SCOPE BUG FIX (5th dispatch, 2026-06-08) — 1 commit, 极小

> **Task 2 4th 派 commit 落 (2 commit: 40311f3 + 5d4db6c), 但 parent 验 pnpm test 抓 3 fail. 根因: `extractSymbols` 把 `file` 字段 误 填 到 `scope` 字段. `Symbol.scope` 应是 parent class/function name (top-level = undefined, method-in-class = 'Outer'), 不 是 file. 1 commit fix.**

## Bug 详情 (parent 验, plan 头部)

**FAIL** `packages/code-intel/test/unit/symbols.test.ts > extracts a top-level function (TS)`
- expect: `fn!.scope` === `undefined`
- got:    `fn!.scope` === `'inline.ts'`

**FAIL** `extracts a class (TS)`
- expect: `cls!.scope` === `undefined`
- got:    `cls!.scope` === `'inline.ts'`

**FAIL** `extracts a method with the enclosing class as scope (nested scope)`
- expect: `m!.scope` === `'Outer'`
- got:    `m!.scope` === `'inline.ts'`

**根因**: `extractSymbols` impl 把 `file` 字段值 误 填 到 `scope` 字段. 应 走 parent AST node (class/function) 派生 scope, 0 走 file.

## HARDEST 拍板 (同 4th 派, 0 任何 scratch/verify/external)

1. **0 写 任何 文件** outside `packages/code-intel/src/symbols.ts`.
   - NO `/tmp` / `os.tmpdir` / `mkdtempSync` / `Temp` / `*scratch*` / `*probe*` / `*debug*` / `*verify*`
   - NO `external_directory` 操作
2. **0 任何 验证** (no pnpm test, no pnpm tsc, no node, no npm).
3. **0 跑 shell 任何 < 5 行命令** (verifications 走 parent).
4. **0 修改**:
   - `repl.ts` / `verify/index.ts` / `verify-runner.test.ts` / `core/src/i18n/index.ts`
   - 任何 test 文件 (test 已 写对, fix impl)
   - 任何 其他 package
   - `package.json` / `tsconfig.json` / `pnpm-workspace.yaml` / root configs
5. **1 commit ONLY** (excatly 1).
6. **0 bump version**, 0 push, 0 tag, 0 publish.

## 起点状态 (4th 派 留下)

```
5d4db6c test(code-intel/symbols): 4 case func / class / import / nested scope (D-32.1)
40311f3 feat(code-intel/symbols): extractSymbols 6 language AST 抽 (D-32.1)
7479398 docs(plans): D-32.1 4th dispatch plan
7b18555 docs/plans): D-32.1 3rd dispatch plan
632c78c test(code-intel/parser): 4 case (Task 1 完)
```

## Task: Fix `extractSymbols` scope 字段

**Files**:
- Modify: `packages/code-intel/src/symbols.ts` (1 file only)

**Fix 协议**:
- `scope` 应 = parent class/function/method name (top-level = undefined)
- 不 再 = file
- 走 AST parent chain 找 最近 class/function/method node, 用 它 的 name 字段 填 scope
- Top-level symbols: parent = root (no class/function), scope = undefined
- Method in class: parent = class_declaration, scope = class name
- Method in function: parent = function_declaration, scope = function name
- Function in function: parent = inner function, scope = inner function name (跟 1st 父 一致)

**Commit**: `fix(code-intel/symbols): scope 改 parent name, 非 filename (D-32.1.2 follow-up)`

## Acceptance criteria (parent will verify, 1 commit only)

- [ ] `git log --oneline 5d4db6c..HEAD` shows **exactly 1 commit** with EXACT message
- [ ] `pnpm test --run packages/code-intel/` ALL 8 test pass (4 parser + 4 symbols)
- [ ] `pnpm tsc -b` 0 new errors
- [ ] 5 红线 0 改
- [ ] `grep -E "os\.tmpdir|mkdtempSync|/tmp" packages/code-intel/src/symbols.ts` = empty
- [ ] No modification outside `packages/code-intel/src/symbols.ts`
- [ ] No bump, no push, no tag, no publish

## DO NOT DO (HARDEST)

- DO NOT write any file outside `packages/code-intel/src/symbols.ts`
- DO NOT use `/tmp` / `os.tmpdir()` / `mkdtempSync` / `Temp`
- DO NOT write scratch / probe / debug / verify files
- DO NOT run any verification (pnpm test, tsc, node, npm, git diff, grep, git status)
- DO NOT modify 5 红线 files
- DO NOT modify test files (test is correct, fix impl)
- DO NOT modify any other package
- DO NOT bump version
- DO NOT push, tag, publish
- DO NOT add more than 1 commit
- DO NOT try to verify (parent will run pnpm test)

## 若 撞 env block 协议

If you hit "permission requested: external_directory" or "/tmp not found":
- **STOP IMMEDIATELY**
- Report the error
- Commit what you have
- Exit
- DO NOT try workarounds (NO mv /tmp, NO export TMPDIR, NO set TEMP)
- DO NOT re-attempt the failed operation

## Pitfalls (opencode 拍板 防翻车, 5th run 重点)

1. **0 写 scratch** (CRITICAL). 写完 fix **直接 commit + exit**.
2. **`/tmp` 0 引用** (CRITICAL).
3. **external_directory 0 操作** (CRITICAL).
4. **若 撞 perm 拒**: STOP, report, commit, exit. **不** workaround.
5. **fix 极小**: 1 字段值 derivation 改. **不** 重写 整个 extractSymbols.
6. **Commit 数量**: EXACTLY 1.
7. **Commit message**: EXACT 字面 match, 不可 改.
