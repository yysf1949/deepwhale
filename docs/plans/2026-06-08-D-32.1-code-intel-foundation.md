# D-32.1 Code Intelligence Foundation (2026-06-08)

> **D-32.1 是 D-32 v1.5 Code Intelligence 第 1 sub-sprint (3 中 第 1)**. 估 19-23 commit, +80-120 test, ~60-90 min opencode. 5 红线 0 触碰, ship v2.0.0-alpha (5 packages + 1 new `@deepwhale/code-intel`).

## Context

**D-31 收口 (release/v1.0 @ 73e7646)**: 33 tool, 12 UI, 1 skill, 921/0/5 test, 5 红线 0 改, 1.0.17 ready 未 publish.

**D-32 主题** (D-32 master plan): v1.5 Code Intelligence = **P0 Kill Gate** per ROADMAP §2. 不做 v1.5 不让 v2.0 开始.

**D-32.1 目标**: Code Intelligence 基础层. 创 `@deepwhale/code-intel` package (Tree-sitter 集成 + Symbol 抽取) + 4 new tool (parse_file / get_symbols / analyze_repo / find_definition) + 1 UI (SymbolTree) + 1 slash (/explore) + registry 装 + count test 升.

**Tech Stack**:
- tree-sitter (^0.21) + tree-sitter-typescript + tree-sitter-javascript + tree-sitter-python + tree-sitter-go + tree-sitter-bash
- wasm 版 (cross-platform, 无 native build)
- vitest, ink-testing-library

## 拍板 (pre-resolved, 不再问)

- 5 packages + 1 new v2.0.0 (master 在 final ship marker 一次性 bump, **不在 1st sub-sprint bump**)
- code-intel 是 **新 package**, 跟 core / edit-engine / llm / tui-ink / coding-agent 同 monorepo
- Tree-sitter 用 **wasm** 版 (cross-platform, Windows 兼容, 无 node-gyp 问题)
- 6 language baseline: typescript / javascript / python / go / rust / bash
- 单文件 find_definition (D-32.2 拍 跨文件 ref)
- SymbolTree UI 跟 WebResultView / ProfileSwitcher 同 pattern (round border + 颜色)
- 0 业务改业务, 5 红线 0 触碰
- **Opencode WILL NOT push / tag / publish** (parent 验 + 推)

## Tasks (TDD: red → green, exact commit msg per task)

### Task 1: 创 `@deepwhale/code-intel` package + parser.ts
**Files**:
- Create: `packages/code-intel/package.json` (name, version "2.0.0", type module, exports)
- Create: `packages/code-intel/src/parser.ts` (parseFile(path) → AST)
- Create: `packages/code-intel/src/languages.ts` (extension → tree-sitter grammar)
- Create: `packages/code-intel/src/index.ts` (public API)
- Test: `packages/code-intel/test/unit/parser.test.ts`
- Test: `packages/code-intel/test/unit/languages.test.ts`

**实现要点**:
- `parseFile(path: string): Promise<{ tree: Tree, language: string, source: string }>`
- Lazy init parser pool (单例)
- 6 language 支持
- Error handling: file-not-found / unsupported-lang / parse-error

**Commit 1**: `feat(monorepo): 创 @deepwhale/code-intel v2.0.0 (D-32.1, tree-sitter 集成)`
**Commit 2**: `feat(code-intel/parser): parseFile + 6 language lazy init (D-32.1)`
**Commit 3**: `test(code-intel/parser): 4 case parse / lang-detect / not-found / unsupported (D-32.1)`

### Task 2: code-intel/src/symbols.ts (Symbol 抽取)
**Files**:
- Create: `packages/code-intel/src/symbols.ts` (extractSymbols(ast, lang) → Symbol[])
- Test: `packages/code-intel/test/unit/symbols.test.ts`

**Symbol shape**:
```ts
export interface Symbol {
  name: string;
  kind: 'function' | 'class' | 'method' | 'variable' | 'import' | 'export' | 'type';
  line: number;
  col: number;
  endLine: number;
  endCol: number;
  scope?: string; // parent class/function name
  file: string;
}
```

**Commit 4**: `feat(code-intel/symbols): extractSymbols 6 language AST 抽 (D-32.1)`
**Commit 5**: `test(code-intel/symbols): 4 case func / class / import / nested scope (D-32.1)`

### Task 3: parse_file tool (D-32.1.1)
**Files**:
- Create: `packages/coding-agent/src/tools/parse-file.ts` (TDD)
- Test: `packages/coding-agent/test/unit/parse-file.test.ts`

**Schema**:
- action: 'summary' | 'ast' | 'symbols'
- path: string
- 输出: `{ success, content, meta: { language, symbolCount } }`

**Commit 6**: `test(coding-agent): parse_file tool 3 mode (D-32.1.1)`
**Commit 7**: `feat(coding-agent/parse-file): 3 mode 走 @deepwhale/code-intel (D-32.1.1)`

### Task 4: get_symbols tool (D-32.1.2)
**Files**:
- Create: `packages/coding-agent/src/tools/get-symbols.ts`
- Test: `packages/coding-agent/test/unit/get-symbols.test.ts`

**Schema**:
- path: string
- kind?: SymbolKind (filter)
- 输出: Symbol[]

**Commit 8**: `test(coding-agent): get_symbols tool kind filter (D-32.1.2)`
**Commit 9**: `feat(coding-agent/get-symbols): 走 @deepwhale/code-intel/extractSymbols (D-32.1.2)`

### Task 5: analyze_repo tool (D-32.1.3)
**Files**:
- Create: `packages/coding-agent/src/tools/analyze-repo.ts`
- Test: `packages/coding-agent/test/unit/analyze-repo.test.ts`

**输出**:
- `{ totalFiles, langStats: { lang: count }, symbolCount, topSymbols: Symbol[] }`

**Commit 10**: `test(coding-agent): analyze_repo tool lang stats (D-32.1.3)`
**Commit 11**: `feat(coding-agent/analyze-repo): walk + 抽 symbols + 统计 (D-32.1.3)`

### Task 6: find_definition tool (D-32.1.4)
**Files**:
- Create: `packages/coding-agent/src/tools/find-definition.ts`
- Test: `packages/coding-agent/test/unit/find-definition.test.ts`

**Schema**:
- symbol: string
- path: string (single file)
- 输出: `{ file, line, col, kind, scope } | null`

**Commit 12**: `test(coding-agent): find_definition tool single file (D-32.1.4)`
**Commit 13**: `feat(coding-agent/find-definition): single-file symbol search (D-32.1.4)`

### Task 7: SymbolTree UI (D-32.1.5)
**Files**:
- Create: `packages/tui-ink/src/components/SymbolTree.tsx`
- Test: `packages/tui-ink/test/symboltree.test.tsx`

**Props**:
- symbols: ReadonlyArray<Symbol>
- maxDepth?: number

**Commit 14**: `feat(tui-ink): SymbolTree 组件 (file→class→method 层级, D-32.1.5)`
**Commit 15**: `test(tui-ink): SymbolTree render / empty / nested (D-32.1.5)`

### Task 8: /explore slash command (D-32.1.6)
**Files**:
- Create: `packages/coding-agent/src/slash/explore.ts`
- Test: `packages/coding-agent/test/unit/slash/explore.test.ts`

**Behavior**:
- input: file path
- 调用 parse_file + get_symbols + SymbolTree 渲染

**Commit 16**: `feat(coding-agent/slash): /explore <file> 跑 parse + symbols + SymbolTree (D-32.1.6)`
**Commit 17**: `test(coding-agent/slash): /explore cmd structure (D-32.1.6)`

### Task 9: registry 装 4 new tool + count test (D-32.1.7)
**Files**:
- Modify: `packages/coding-agent/src/tools/registry.ts` (import 4 new + register)
- Modify: 5 count test 文件 (升 33 → 37)

**Commit 18**: `feat(coding-agent/registry): 装 4 code-intel tools (33 → 37, D-32.1.7)`
**Commit 19**: `test(coding-agent): 5 count test 33 → 37 + 4 tool TS strict 守卫 (D-32.1.7)`

### Task 10: 5 红线 preserved + 5 package version bump + ship marker
- 5 红线: `git diff 73e7646..HEAD -- repl.ts verify/index.ts verify-runner.test.ts` = empty
- 5 packages + 1 new (code-intel) version bump 1.0.17 → **2.0.0** (master bump, final ship marker 一次性)

**Commit 20**: `chore(monorepo): 6 packages version 1.0.17 → 2.0.0 (D-32 ship ritual)`
**Commit 21**: `ship(monorepo): D-32.1 收口 (Code Intelligence Foundation, code-intel 创, 4 tool + 1 UI + 1 slash, 33→37 tools, 12→13 UI, 14→15 slash, 921+~80-120 = ~1000-1040 pass, 5 红线 0 改, 6 packages 2.0.0 ready, v1.5-alpha 拍板)`

## Acceptance criteria (parent will verify)

- [ ] `git log --oneline 73e7646..HEAD` shows 21 commits with EXACT messages
- [ ] `pnpm test` passes: 921 + ~80-120 = ~1000-1040 (估, 实际 看 opencode output)
- [ ] `pnpm tsc -b` 0 new errors
- [ ] 5 红线 0 改 (grep diff = 0 on 3 files)
- [ ] 6 packages version = "2.0.0" (core, edit-engine, llm, tui-ink, coding-agent, code-intel)
- [ ] New package `@deepwhale/code-intel` exists with parser/symbols/languages/index
- [ ] 4 new tools (parse-file, get-symbols, analyze-repo, find-definition) registered in registry (size 37)
- [ ] SymbolTree component renders via ink-testing-library
- [ ] `/explore` slash command registered
- [ ] No modification to: `repl.ts`, `verify/index.ts`, `verify-runner.test.ts`, `core/src/i18n/index.ts`
- [ ] No push, no tag, no publish (parent's job)

## DO NOT DO (per D-30 协议)

- DO NOT push to origin
- DO NOT tag
- DO NOT run `pnpm publish`
- DO NOT modify 5 红线 files
- DO NOT bump version in 5 packages **until final ship marker** (commit 20)
- DO NOT skip TDD (red → green per task)
- DO NOT merge / consolidate commits (each task = exactly 1-2 commits, exact msg)
- DO NOT add extra commits (must be 21, exact messages)
- DO NOT add npm deps not in this list (tree-sitter + 6 grammar packs only)

## Pitfalls (opencode 拍板 防翻车)

1. **tree-sitter wasm 加载**: 用 `Parser` + `Language.load(path)` 而非 `Language.load(languageObject)`. Windows path separator 注意.
2. **6 grammar npm 装**: `tree-sitter-{typescript,javascript,python,go,bash}` + `tree-sitter-rust` (rust 用 `tree-sitter-rust`). Total 6 deps.
3. **package.json exports**: `"./*": { "import": "./dist/*.js" }` (跟 core 一致).
4. **TS strict 守卫**: 5 count test 升 时, 4 new tool schema 必须 TS strict pass.
5. **SymbolTree render**: 用 `ink-testing-library` 跟 D-31.4 TUI probe 一致.
6. **Opencode may skip "duplicate" tasks**: 如果某 5 count test 已升 33, 不要重 升. 直接 改 37.
7. **Window opencode 10-min timeout**: master plan 估 60-90 min, **必 background + notify_on_complete** (D-30 教训).
