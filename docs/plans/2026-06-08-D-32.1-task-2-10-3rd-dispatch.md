# D-32.1 Code Intelligence Foundation (3rd dispatch, 2026-06-08) — Task 2-10 only, 强拍板 0 scratch

> **2nd opencode run 完 Task 1 (3 commit ✓) 后 想 走 scratch probe 验, 撞 /tmp env block (Windows) + external_directory 权限. Production code 全干净. 父 session 强 拍板: 0 scratch / 0 /tmp / 0 external dir. 验证 只 走 `pnpm test` + `pnpm tsc -b` + `git diff` + `grep` 4 件套.**

## 父 session 强 拍板 (HARD, 违返 = session 失败)

1. **0 写 scratch / probe / debug script** (e.g. `probe.mjs`, `test-sandbox.mjs`, `debug.mjs`, `verify.mjs`, `tmp.mjs`).
   - 任何验证 走 `pnpm test` 或 `pnpm tsc -b` 或 `git diff` 或 `grep` 或 `git status`.
   - **不**许 `node <some-script>.mjs` 跑临时文件验.
   - **不**许 `cat > foo.mjs` 创临时文件.

2. **0 用 `/tmp` 路径**. Windows 无 `/tmp` (C:\Users\butterfly443\AppData\Local\Temp\ 是 Windows 临时, 跟 /tmp 无关).
   - 测试 fixture 放 `packages/code-intel/test/fixtures/` (committed to repo).
   - **不**许 `os.tmpdir()` 在 source / test 里 (D-32.1 plan 头部 拍板).
   - **不**许 `mkdtempSync` / `tmpdir` / `Temp` / `os.tmpdir` / `/tmp` 任何 引用.

3. **0 `external_directory` 操作** (C:\Users\butterfly443\AppData\Local\Temp\*).
   - **不**许 `npm pack` / `npm view` 在 scratch (versions 父 已 验, 在 FOLLOWUP plan 头部).
   - **不**许 写 `C:\Users\butterfly443\AppData\Local\Temp\*` 任何文件.
   - **不**许 创 `~/.cache/...` / `~/AppData/Local/Temp/...` 任何临时文件.

4. **验证 协议** (单 1 步):
   - 写 code 完 → `pnpm test --run` 跑 (vitest picks up via `test/**/*.test.ts` glob) → 失败 fix → 绿 commit
   - 0 中间 sandbox 验, 0 二次 验 (避免 撞 env)

## 起点状态 (2nd run 留下)

```
632c78c test(code-intel/parser): 4 case parse / lang-detect / not-found / unsupported (D-32.1)
99e37c9 feat(code-intel/parser): parseFile + 6 language lazy load (D-32.1)
d2a24e7 feat(monorepo): 创 @deepwhale/code-intel v2.0.0 (D-32.1, web-tree-sitter 集成)
da50489 docs(plans): D-32.1 FOLLOW-UP plan ...
31fd70d docs(plans): D-32 master + D-32.1 sub-plan ...
73e7646 D-31.4.8 i18n fix
```

**Task 1 ✓ 完** (创 code-intel package + parser.ts + 6 language lazy load + 4 test). 剩 Task 2-10 (18 commit).

## Tasks 2-10 (TDD red → green, 18 commit exact msg)

### Task 2: code-intel/src/symbols.ts (Symbol 抽取)
**Files**:
- Modify: `packages/code-intel/src/symbols.ts` (创)
- Modify: `packages/code-intel/test/unit/symbols.test.ts` (创)
- Modify: `packages/code-intel/src/index.ts` (export symbols)

**Symbol shape** (跟原 plan 1:1):
```ts
export interface Symbol {
  name: string;
  kind: 'function' | 'class' | 'method' | 'variable' | 'import' | 'export' | 'type';
  line: number;
  col: number;
  endLine: number;
  endCol: number;
  scope?: string;
  file: string;
}
```

**实现要点**:
- 走 tree-sitter query (per language, 6 query file OR 1 file 跟 lang switch)
- query file 放 `src/queries/<lang>.scm`
- 6 language 至少 function / class / import 3 kind
- 4 test: func / class / import / nested scope

**Commit 4**: `feat(code-intel/symbols): extractSymbols 6 language AST 抽 (D-32.1)`
**Commit 5**: `test(code-intel/symbols): 4 case func / class / import / nested scope (D-32.1)`

### Task 3: parse_file tool (D-32.1.1)
**Files**:
- Create: `packages/coding-agent/src/tools/parse-file.ts`
- Test: `packages/coding-agent/test/unit/parse-file.test.ts`

**Schema**:
- action: 'summary' | 'ast' | 'symbols'
- path: string
- 输出: `{ success, content, meta: { language, symbolCount } }`

**Commit 6**: `test(coding-agent): parse_file tool 3 mode (D-32.1.1)`
**Commit 7**: `feat(coding-agent/parse-file): 3 mode 走 @deepwhale/code-intel (D-32.1.1)`

### Task 4: get_symbols tool (D-32.1.2)
**Files**:
- Create: `packages/coding-agent/src/tools/get-symbols.ts`
- Test: `packages/coding-agent/test/unit/get-symbols.test.ts`

**Schema**:
- path: string
- kind?: SymbolKind
- 输出: Symbol[]

**Commit 8**: `test(coding-agent): get_symbols tool kind filter (D-32.1.2)`
**Commit 9**: `feat(coding-agent/get-symbols): 走 @deepwhale/code-intel/extractSymbols (D-32.1.2)`

### Task 5: analyze_repo tool (D-32.1.3)
**Files**:
- Create: `packages/coding-agent/src/tools/analyze-repo.ts`
- Test: `packages/coding-agent/test/unit/analyze-repo.test.ts`

**输出**:
- `{ totalFiles, langStats: { lang: count }, symbolCount, topSymbols: Symbol[] }`

**Commit 10**: `test(coding-agent): analyze_repo tool lang stats (D-32.1.3)`
**Commit 11**: `feat(coding-agent/analyze-repo): walk + 抽 symbols + 统计 (D-32.1.3)`

### Task 6: find_definition tool (D-32.1.4)
**Files**:
- Create: `packages/coding-agent/src/tools/find-definition.ts`
- Test: `packages/coding-agent/test/unit/find-definition.test.ts`

**Schema**:
- symbol: string
- path: string (single file)
- 输出: `{ file, line, col, kind, scope } | null`

**Commit 12**: `test(coding-agent): find_definition tool single file (D-32.1.4)`
**Commit 13**: `feat(coding-agent/find-definition): single-file symbol search (D-32.1.4)`

### Task 7: SymbolTree UI (D-32.1.5)
**Files**:
- Create: `packages/tui-ink/src/components/SymbolTree.tsx`
- Test: `packages/tui-ink/test/symboltree.test.tsx`

**Props**: symbols, maxDepth

**Commit 14**: `feat(tui-ink): SymbolTree 组件 (file→class→method 层级, D-32.1.5)`
**Commit 15**: `test(tui-ink): SymbolTree render / empty / nested (D-32.1.5)`

### Task 8: /explore slash command (D-32.1.6)
**Files**:
- Create: `packages/coding-agent/src/slash/explore.ts`
- Test: `packages/coding-agent/test/unit/slash/explore.test.ts`

**Commit 16**: `feat(coding-agent/slash): /explore <file> 跑 parse + symbols + SymbolTree (D-32.1.6)`
**Commit 17**: `test(coding-agent/slash): /explore cmd structure (D-32.1.6)`

### Task 9: registry 装 4 new tool + count test (D-32.1.7)
**Files**:
- Modify: `packages/coding-agent/src/tools/registry.ts`
- Modify: 5 count test 文件 (升 33 → 37)

**Commit 18**: `feat(coding-agent/registry): 装 4 code-intel tools (33 → 37, D-32.1.7)`
**Commit 19**: `test(coding-agent): 5 count test 33 → 37 + 4 tool TS strict 守卫 (D-32.1.7)`

### Task 10: 5 红线 preserved + 6 packages version bump + ship marker
- 5 红线: `git diff 73e7646..HEAD -- repl.ts verify/index.ts verify-runner.test.ts` = empty
- 6 packages version bump 1.0.17 → **2.0.0**

**Commit 20**: `chore(monorepo): 6 packages version 1.0.17 → 2.0.0 (D-32 ship ritual)`
**Commit 21**: `ship(monorepo): D-32.1 收口 (Code Intelligence Foundation, code-intel 创, 4 tool + 1 UI + 1 slash, 33→37 tools, 12→13 UI, 14→15 slash, 921+~80-120 = ~1000-1040 pass, 5 红线 0 改, 6 packages 2.0.0 ready, v1.5-alpha 拍板)`

## Acceptance criteria (parent will verify, Task 2-10 only)

- [ ] `git log --oneline 632c78c..HEAD` shows 18 commits with EXACT messages
- [ ] `pnpm test` passes: 921 + ~80-120 = ~1000-1040
- [ ] `pnpm tsc -b` 0 new errors
- [ ] 5 红线 0 改 (grep diff = 0 on 3 files)
- [ ] 6 packages version = "2.0.0" (code-intel, core, edit-engine, llm, tui-ink, coding-agent)
- [ ] 4 new tools (parse-file, get-symbols, analyze-repo, find-definition) registered in registry (size 37)
- [ ] SymbolTree component renders via ink-testing-library
- [ ] `/explore` slash command registered
- [ ] 6 test fixtures committed in `packages/code-intel/test/fixtures/` (Task 1 已可能创, verify)
- [ ] `grep -r "/tmp\|os.tmpdir\|mkdtempSync\|Temp" packages/code-intel/src packages/code-intel/test packages/coding-agent/src/tools/parse-file.ts packages/coding-agent/src/tools/get-symbols.ts packages/coding-agent/src/tools/analyze-repo.ts packages/coding-agent/src/tools/find-definition.ts packages/coding-agent/src/slash/explore.ts` = empty
- [ ] `grep -r "probe\|scratch\|debug.mjs" packages/` = empty
- [ ] No modification to: `repl.ts`, `verify/index.ts`, `verify-runner.test.ts`, `core/src/i18n/index.ts`
- [ ] No push, no tag, no publish

## DO NOT DO (HARD, 拍板 强化)

- DO NOT push to origin
- DO NOT tag
- DO NOT run `pnpm publish`
- DO NOT modify 5 红线 files
- DO NOT bump version in 5 packages **until final ship marker** (commit 20)
- DO NOT skip TDD (red → green per task)
- DO NOT merge / consolidate commits
- DO NOT add extra commits (must be 18, exact messages)
- DO NOT use `tree-sitter` native package (use `web-tree-sitter` WASM)
- DO NOT use `/tmp` for test fixtures
- DO NOT use `os.tmpdir()` / `mkdtempSync` / `Temp`
- DO NOT write any scratch / probe / debug script
- DO NOT run `node <some-script>.mjs` 验
- DO NOT use `external_directory` 操作 (AppData/Local/Temp/*)
- DO NOT use `npm pack` to inspect packages

## Pitfalls (opencode 拍板 防翻车, 3rd run 重点)

1. **0 写 scratch script** (CRITICAL — 2nd run 在这 撞 /tmp). 验证 **只** 走 `pnpm test --run` + `pnpm tsc -b`. **不** 写 `probe.mjs` / `test-sandbox.mjs` / `verify.mjs`.
2. **`/tmp` 0 引用** (CRITICAL). 不在 任何 source / test / script 用 `/tmp` 或 `os.tmpdir()`. 测试 fixture 在 committed `packages/code-intel/test/fixtures/`.
3. **external_directory 0 操作** (CRITICAL). 不动 C:\Users\butterfly443\AppData\Local\Temp\* 或 ~/.cache 或 任何 OS 临时目录.
4. **`Parser.init()` 在 module-load 时跑 1 次** (ESM top-level await OK).
5. **`Language.load(wasmPath)` 用 `createRequire(import.meta.url).resolve`** (而非 `import.meta.resolve`).
6. **6 grammar packs 都需 tree-sitter-* npm name**. 6 .wasm 文件 path 各自.
7. **5 红线 preserved**: 不动 `repl.ts` / `verify/index.ts` / `verify-runner.test.ts`.
8. **Opencode 10-min timeout**: 18 commit 估 50-70 min, **必 background + notify_on_complete**.
9. **version bump 集中 commit 20** (master bump, 6 packages 一起, 0 业务改).
10. **5 红线 preserved at task 10** ship marker 验 (commit msg 必含 "5 红线 0 改").
