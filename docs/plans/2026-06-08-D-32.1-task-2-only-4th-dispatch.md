# D-32.1 Task 2 (SINGLE TASK, 2 commit, 4th dispatch, 2026-06-08) — symbols.ts only

> **3rd opencode run 又 撞 /tmp env block + external_directory 拒. 0 commit, 0 进展. 父 session 试 4th dispatch 极 小 scope (1 task, 2 commit, symbols.ts only) + 极 显式 override 拍板 "0 任何 scratch verify". 若 还 撞, 父 session 直接 自己 写 code.**

## HARDEST 拍板 (违 1 个 = 失败, 父 自己 写)

1. **0 写 任何 文件** outside `packages/code-intel/{src,test/unit}` (the 2 dir you 改).
   - NO `/tmp/*` (Windows 无 /tmp, 等价 `C:\Users\butterfly443\AppData\Local\Temp\*`, 也 forbidden)
   - NO `C:\Users\butterfly443\AppData\Local\Temp\*` (external_directory, permission auto-reject)
   - NO `~/.cache/*`
   - NO `C:\tmp\*`
   - NO `c:\Users\BUTTER~1\AppData\Local\Temp\*`
   - NO `os.tmpdir()` / `mkdtempSync` / `mktemp*` / `Temp` 任何 引用
   - NO `*scratch*` / `*probe*` / `*debug*` / `*test-sandbox*` 文件名 任何 地方
   - NO `node <some-script>.mjs` 跑 (无论 mjs 哪里)

2. **0 任何 scratch verify**. **写完 code 直接 exit, parent 验**.
   - 验证 协议 = parent 走 (`pnpm test --run` + `pnpm tsc -b` + `git diff` + `grep` 4 件套)
   - 你 **不** 跑 test, **不** 跑 tsc, **不** 跑 node
   - 你 **不** 试 自己 验 "web-tree-sitter 装好" — parent 已 验 (FOLLOWUP plan 头部)
   - 你 **不** npm pack, **不** curl, **不** wget
   - 你 **不** 试 import / require test code 验

3. **0 调 shell 用 < 5 行的命令**. 任何 `pnpm test` / `pnpm tsc -b` / `git diff` / `grep` / `git status` 都 由 parent 跑, **不** 你 跑.

4. **0 修改**:
   - `repl.ts` / `verify/index.ts` / `verify-runner.test.ts` (5 红线)
   - `core/src/i18n/index.ts`
   - 任何 `package.json` 不在 Task 2 列
   - `pnpm-workspace.yaml` (WASM 不 build, 无需改)
   - `tsconfig.json` (code-intel 已 ref)
   - 任何 其他 package

## 起点状态 (3rd run 留下, 0 进展)

```
7b18555 docs(plans): D-32.1 3rd dispatch plan ...
632c78c test(code-intel/parser): 4 case parse / lang-detect / not-found / unsupported (D-32.1)
99e37c9 feat(code-intel/parser): parseFile + 6 language lazy load (D-32.1)
d2a24e7 feat(monorepo): 创 @deepwhale/code-intel v2.0.0 (D-32.1, web-tree-sitter 集成)
da50489 docs(plans): D-32.1 FOLLOW-UP plan
31fd70d docs(plans): D-32 master + D-32.1 sub-plan
73e7646 D-31.4.8 i18n fix
```

Task 1 ✓ 完 (3 commit, code-intel package 创, parser.ts + 4 test). 剩 Task 2-10 (18 commit).

## Task 2: code-intel/src/symbols.ts (Symbol 抽取) — ONLY THIS

**Files**:
- Create: `packages/code-intel/src/symbols.ts` (创)
- Create: `packages/code-intel/test/unit/symbols.test.ts` (创)
- Modify: `packages/code-intel/src/index.ts` (export symbols)

**Symbol shape** (跟原 plan 1:1):
```ts
export interface Symbol {
  name: string;
  kind: 'function' | 'class' | 'method' | 'variable' | 'import' | 'export' | 'type';
  line: number;
  col: number;
  endLine: number;
  endCol: number;
  scope?: string;
  file: string;
}
```

**实现要点**:
- 走 tree-sitter query (per language, 6 query file OR 1 file 跟 lang switch)
- query file 放 `src/queries/<lang>.scm` OR inline switch
- 6 language 至少 function / class / import 3 kind
- 4 test: func / class / import / nested scope

**Commit 4 (Task 2)**: `feat(code-intel/symbols): extractSymbols 6 language AST 抽 (D-32.1)`
**Commit 5 (Task 2)**: `test(code-intel/symbols): 4 case func / class / import / nested scope (D-32.1)`

## Acceptance criteria (parent will verify, Task 2 only)

- [ ] `git log --oneline 632c78c..HEAD` shows **exactly 2 commits** with EXACT messages
- [ ] `pnpm test --run packages/code-intel/` passes (4 new test 绿)
- [ ] `pnpm tsc -b` 0 new errors
- [ ] 5 红线 0 改 (grep diff = 0 on 3 files)
- [ ] `grep -r "os.tmpdir\|mkdtempSync\|/tmp" packages/code-intel/src/symbols.ts packages/code-intel/test/unit/symbols.test.ts packages/code-intel/src/index.ts` = empty
- [ ] `ls packages/code-intel/src/ | grep -E "scratch|probe|debug"` = empty
- [ ] No scratch files anywhere in working tree
- [ ] No modification to: `repl.ts`, `verify/index.ts`, `verify-runner.test.ts`, `core/src/i18n/index.ts`, `tsconfig.json`, `pnpm-workspace.yaml`, root `package.json`
- [ ] No push, no tag, no publish

## DO NOT DO (HARDEST, 4th 派 specific)

- DO NOT write any file outside `packages/code-intel/{src,test/unit,index.ts}`
- DO NOT use `/tmp` (Windows 无)
- DO NOT use `os.tmpdir()` / `mkdtempSync` / `Temp` any form
- DO NOT write `*scratch*` / `*probe*` / `*debug*` / `*verify*` files
- DO NOT run `node <file>.mjs` or `node <file>.js` or `node <file>.ts`
- DO NOT run `pnpm test` or `pnpm tsc -b` (parent will)
- DO NOT run `npm pack` / `npm view` / `curl` / `wget`
- DO NOT touch `repl.ts` / `verify/index.ts` / `verify-runner.test.ts` / `core/src/i18n/index.ts` / `tsconfig.json` / `pnpm-workspace.yaml` / root `package.json`
- DO NOT bump version
- DO NOT push, tag, publish
- DO NOT add more than 2 commits (Task 2 = exactly 2 commits)
- DO NOT merge / consolidate commits
- DO NOT add scratch verify step

## 若 4th 还 撞 env block 协议

If you (opencode) encounter any "permission requested: external_directory" or "/tmp not found" error:
- **STOP IMMEDIATELY**
- Report the error in your final summary
- DO NOT try workarounds (NO mv /tmp / elsewhere, NO export TMPDIR=, NO set TEMP=)
- DO NOT try to write to other temp paths
- DO NOT re-attempt the failed operation

If the operation cannot complete without scratch:
- Write the code changes anyway (the parent will verify in a different way)
- Commit (if code is complete and tests will pass)
- Exit

## Pitfalls (opencode 拍板 防翻车, 4th run 重点)

1. **0 写 scratch script** (CRITICAL — 3 次 都 在这 撞). 写 code 完 **直接 exit**.
2. **`/tmp` 0 引用** (CRITICAL). 不在 任何 source / test 用 `/tmp` 或 `os.tmpdir()`.
3. **external_directory 0 操作** (CRITICAL). 不动 AppData/Local/Temp/* 或 ~/.cache.
4. **若 撞 perm 拒**: STOP, report, 写 code anyway, commit, exit. **不** workaround.
5. **Opencode 10-min timeout**: 2 commit 估 5-10 min, **必 background + notify_on_complete**.
6. **Commit 数量**: EXACTLY 2, no more, no less.
7. **Commit message**: EXACT 字面 match, 不可 改.
