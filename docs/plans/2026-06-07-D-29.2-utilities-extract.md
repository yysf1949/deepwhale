# D-29.2 repl.ts Utilities Extract — Implementation Plan

> **For implementer:** Refactor 抽 3 模块, 每 commit 跑 `pnpm test` 验 0 new fail (vs
> d646af0 baseline 628 passed / 5 skipped, 1 偶发 flaky 跟本 PR 无关). **TDD 不适用**
> — 行为 1:1 保 628 既有测试, **不**写新测试 (跟 D-29.1.1 一致: 单测覆盖
> 行为 1:1 保, 红线行为靠集成测).
>
> **本文件**只覆盖 D-29.2 (utilities cluster), 后续 D-29.3+ (startRepl 内部 line
> handler / close handler / finish() 抽) 在子文档中追加.

**Goal:** 把 `packages/coding-agent/src/repl.ts` 754L 内的 3 个独立职责模块
(`runAgentTurn` 150L + `formatError` 27L + `makeLlmSummarizeFn` 32L + `appendStepSummary`
14L = **223L**) 抽到 `src/repl/*.ts` 子目录, repl.ts 缩到 ~550L, 5 红线 0 改, 公共
API 0 改, 628 既有测试 0 new fail.

**Architecture:** 单向依赖 — `repl.ts` 持有 runAgentTurn / formatError / makeLlmSummarizeFn
/ appendStepSummary 的 import 调用, 行为 1:1, 公共 API re-export 保 startRepl / runOneTurn
/ formatUsageStatus / createReplConfirm / runAgentTurn (新增 export). 子模块采用
**纯函数 / 工厂**形态 (跟 `repl-confirm.ts` / `repl-signal-coordinator.ts` 同形态).

**Tech Stack:** TypeScript 5.7 (strict), Node 22 内置 (无新 dep), 跟 `repl-session.ts`
工厂模式对齐.

---

## §1 现状 (release/v1.0 HEAD d646af0)

`packages/coding-agent/src/repl.ts` 754L, 5 红线段 (review blocker, 改任何一段都
怕踩其它 4 段):

| 红线 | 现位置 (估) | 描述 | 拍板来源 |
| --- | --- | --- | --- |
| **D-19 P1/P2** | startRepl 内 (finish 函数) | SIGINT dismiss-then-abort + finish() `process.off` 在 rl.close 之前 | D-19.5 (`bacd09a`) |
| **6afccc8 P1** | startRepl 内 (line handler + finally) | turnInFlight / lineQueue / pendingExit / slash builtin guard | D-19.5 / D-19.6 / D-19.6.1 |
| **1ceef94 P1** | startRepl 内 (slash /verify 路径) | /verify nested try/finally + appendVerificationEvent | D-11-4 |
| **no-unsafe-finally** | startRepl 内 (line handler finally 块) | finally 块 if/else if/else 链, 无 return | D-19.6 |

**5 红线保留** = 本 PR 3 commit 抽 utilities 时**不**改这 4 段内容, 只挪
`runAgentTurn` / `formatError` / `makeLlmSummarizeFn` / `appendStepSummary` 4 个
独立职责. 跑过 `git diff` 验 5 红线行号内的内容**逐字**保持 (允许行号浮动,
内容 0 改).

**公共 API caller map (不能破):**
- `src/index.ts:32` re-export: `startRepl`, `runOneTurn`, `formatUsageStatus`, `createReplConfirm`
- `src/repl.ts:54` re-export: `createReplConfirm` (tui-ink 路径)
- `src/repl.ts:57` re-export: `formatUsageStatus`, `appendUsageStatus`, `UsageEmaState` (D-29.1.2)
- `modes/print.ts:35` (5 caller) — `formatUsageStatus` / `appendUsageStatus`
- `modes/tui.ts:62` (3 caller) — `formatUsageStatus` / `createReplConfirm`
- 6 test 文件 (`test/repl/*.test.ts` + `test/integration/*`)
- **`runAgentTurn` 是 export function** (D-25 起就 export, 单测 `test/unit/repl-verify.test.ts:212`
  调过), 公共 API 必须保

**repl.ts 754L 内部结构:**
| 段 | 行号 | 行数 | 职责 |
| --- | --- | --- | --- |
| Imports | L1-60 | 60 | 模块导入 + 5 子模块 re-export |
| ReplOptions interface | L64-116 | 53 | 公共 API 输入契约 |
| `runOneTurn` | L121-146 | 26 | 单轮 chat 工具函数 (不持久化) |
| `startRepl` | L151-522 | 372 | REPL 主循环 + 5 红线 (本 PR **不动**) |
| `runAgentTurn` | L524-673 | 150 | 单 turn agent 逻辑 (D-29.2.1 抽) |
| `appendStepSummary` | L675-688 | 14 | 步骤摘要 (D-29.2.2 抽) |
| `formatError` | L695-721 | 27 | LLM 错误格式化 (D-29.2.2 抽) |
| `makeLlmSummarizeFn` | L723-754 | 32 | compaction summary 工厂 (D-29.2.2 抽) |

---

## §2 D-29.2.1 runAgentTurn 抽出 (Commit 1)

### §2.1 抽什么 (L524-673, 150L)

```ts
export async function runAgentTurn(
  client: LLMClient,
  userInput: string,
  workingMessages: ChatMessage[],
  writer: SessionWriter | null,
  out: NodeJS.WritableStream,
  err: NodeJS.WritableStream,
  signal: AbortSignal,
  compactionConfig: AgentCompactionConfig | null = null,
  sandboxRunner?: SandboxRunner,
  yes?: boolean,
  policy?: ToolPolicy,
  emaState?: UsageEmaState,
): Promise<void> {
  // 1) persist user, 2) build turnMessages, 3) runToolLoop/WithCompaction, 4) catch,
  // 5) persistToolLoopSteps, 6) update workingMessages, 7) step summary, 8) usage
}
```

### §2.2 抽后接口 (repl-agent-turn.ts)

```ts
export async function runAgentTurn(
  client: LLMClient,
  userInput: string,
  workingMessages: ChatMessage[],
  writer: SessionWriter | null,
  out: NodeJS.WritableStream,
  err: NodeJS.WritableStream,
  signal: AbortSignal,
  compactionConfig: AgentCompactionConfig | null = null,
  sandboxRunner?: SandboxRunner,
  yes?: boolean,
  policy?: ToolPolicy,
  emaState?: UsageEmaState,
): Promise<void> {
  // 1:1 行为保, 函数体逐字迁移
}
```

### §2.3 抽后 repl.ts 改法 (3 处)

1. **L55-58** 附近加 import: `import { runAgentTurn } from './repl/repl-agent-turn.js';`
2. **L54** 下面加 re-export: `export { runAgentTurn } from './repl/repl-agent-turn.js';`
3. **L524-673** 整段删除

### §2.4 严格红线 (0 改)

- **0 改业务逻辑** — runAgentTurn 函数体 1:1, 包括 (1) persist user, (2) build turnMessages, (3) runToolLoop/WithCompaction 入口选择, (4) catch 分支 signal.aborted 优先, (5) persistToolLoopSteps 写, (6) workingMessages 更新, (7) step summary, (8) usage
- **0 改公共 API** — `runAgentTurn` 签名 1:1, 11 个参数顺序/类型/默认值全保
- **0 改 5 红线** — startRepl 内部 4 段红线**不动**
- **0 改 tui.ts / print.ts** — 不动
- **0 改其它测试** — 不加新测试, 不改现有测试
- **0 改 5 packages version** — package.json 不动
- **0 加新依赖** — 用现有 LLMClient / CompactionState / SummarizeFn

### §2.5 验证 (D-29.2.1)

1. `git diff` 5 红线 (startRepl 内 4 段) 内容**逐字**保持
2. `pnpm test` → 0 new fail (vs d646af0 baseline 628/5/0)
3. `git log -1 --stat` 验单 commit `refactor(coding-agent/repl): 抽 runAgentTurn (D-29.2.1)`
4. **不 push, 不 tag** — 只本地 commit, 3 commit 攒齐再 push

---

## §3 D-29.2.2 utilities cluster 抽出 (Commit 2)

### §3.1 抽什么 (3 块共 73L)

**块 A — `formatError` (L695-721, 27L)** — LLM 错误 → i18n 文案映射
**块 B — `makeLlmSummarizeFn` (L723-754, 32L)** — compaction summary 工厂
**块 C — `appendStepSummary` (L675-688, 14L)** — 步骤摘要输出

### §3.2 抽后文件

```
packages/coding-agent/src/repl/repl-format-error.ts        // 块 A
packages/coding-agent/src/repl/repl-compaction-summary.ts  // 块 B
packages/coding-agent/src/repl/repl-step-summary.ts        // 块 C
```

(3 文件拆, 跟 D-29.1.1 ~ 1.3 单职责单文件同节奏. 不合并 — 合并会让单个文件回到
多职责形态, 跟 D-29.1 抽法形态冲突.)

### §3.3 抽后接口 (3 文件)

**repl-format-error.ts:**
```ts
export function formatError(e: unknown): string {
  // 1:1 行为保, 函数体逐字迁移
}
```

**repl-compaction-summary.ts:**
```ts
export function makeLlmSummarizeFn(
  client: LLMClient,
  _protocol: 'openai' | 'anthropic',
): SummarizeFn {
  // 1:1 行为保, 函数体逐字迁移
}
```

**repl-step-summary.ts:**
```ts
export function appendStepSummary(
  step: ToolLoopStep,
  out: NodeJS.WritableStream,
  err: NodeJS.WritableStream,
): void {
  // 1:1 行为保, 函数体逐字迁移
}
```

### §3.4 抽后 repl.ts 改法 (5 处)

1. **L55-58** 附近加 3 import:
   ```ts
   import { formatError } from './repl/repl-format-error.js';
   import { makeLlmSummarizeFn } from './repl/repl-compaction-summary.js';
   import { appendStepSummary } from './repl/repl-step-summary.js';
   ```
2. **runAgentTurn 调用点 (D-29.2.1 后约 L432-440)** — 内部调用 `formatError` /
   `makeLlmSummarizeFn` / `appendStepSummary` 跟新文件 import 路径一致 (runAgentTurn
   抽到 `repl-agent-turn.ts` 后, 这 3 import 由 `repl-agent-turn.ts` 自己持有)
3. **L695-721** `formatError` 函数体删除
4. **L675-688** `appendStepSummary` 函数体删除
5. **L723-754** `makeLlmSummarizeFn` 函数体删除

### §3.5 严格红线 (0 改)

- **0 改业务逻辑** — 3 函数体 1:1, 逐字迁移
- **0 改公共 API** — `formatError` 当前**不**是 public export (repl.ts 内私有),
   `makeLlmSummarizeFn` 当前**不**是 public export, `appendStepSummary` 当前
   **不**是 public export. 抽后保持私有 (不 re-export), 走 module 内 import.
- **0 改 5 红线** — 跟 D-29.2.1 同
- **0 改 tui.ts / print.ts** — 不动
- **0 改其它测试** — 不加新测试
- **0 改 5 packages version** — package.json 不动
- **0 加新依赖** — 用现有 LLMClient / SummarizeFn / ToolLoopStep

### §3.6 验证 (D-29.2.2)

1. `git diff` 5 红线内容**逐字**保持
2. `pnpm test` → 0 new fail (vs d646af0 baseline 628/5/0)
3. `git log -1 --stat` 验单 commit `refactor(coding-agent/repl): 抽 formatError / makeLlmSummarizeFn / appendStepSummary (D-29.2.2)`
4. **不 push, 不 tag**

---

## §4 D-29.2.3 trim + verify 收口 (Commit 3)

### §4.1 抽什么

**无新模块抽** — 本 commit 只做两件事:

1. **行号清理** — D-29.2.1 + D-29.2.2 后, repl.ts 从 754L → ~550L. 修模块顶部
   注释 (ReplOptions / startRepl / runOneTurn 段), 删过时的 line-count 数字 /
   Sprint 引用错位.
2. **README 跟新** — 如果 1.0.13 README 提到 "754L", 跟新到 "550L"
3. **验 0 行号内容逐字** — `git diff main..release/v1.0` 跑过, 5 红线段**逐字**保持

### §4.2 严格红线 (0 改)

- **0 改业务逻辑** — 注释清理
- **0 改公共 API** — 不动
- **0 改 5 红线** — 逐字保持 (允许行号浮动, 内容 0 改)
- **0 改 5 packages version** — package.json 不动 (本 commit 是 refactor, 不升 v)
- **0 改 tui.ts / print.ts / 测试** — 不动

### §4.3 验证 (D-29.2.3)

1. `git diff main..release/v1.0` 跑过 — 5 红线段**逐字**保持
2. `pnpm test` → 0 new fail
3. `git log -3 --oneline` 验 3 commit 顺序
4. **push + 3 件套验**:
   - `git push origin release/v1.0`
   - `git log origin/release/v1.0..release/v1.0` 空
   - 触发 CI (若配置) — 本项目无 CI, 跳过
5. **不 tag, 不 rebase** — 留给 D-29 ship ritual (跟 D-29.1.4 trim 形态一致)

---

## §5 命名约定 (跟现有 repl/*.ts 对齐)

- 文件: `repl-<name>.ts` (kebab-case, 跟 `repl-confirm.ts` / `repl-signal-coordinator.ts` 一致)
- 函数: `formatError` / `makeLlmSummarizeFn` / `appendStepSummary` / `runAgentTurn` (跟现名 1:1)
- 内部: 纯函数 / 工厂, 闭包尽量避免 (除 runAgentTurn 内部 retry / state)

---

## §6 后续 sub-cmd 留给 D-29.3+ (本 PR 不做)

| Sub-cmd | 抽什么 | 文件 | 留待 |
| --- | --- | --- | --- |
| D-29.3.1 | startRepl rl.on('line') handler (128L) | `repl/repl-line-handler.ts` | 后续 |
| D-29.3.2 | startRepl rl.on('close') handler (27L) | `repl/repl-close-handler.ts` | 后续 |
| D-29.3.3 | startRepl finish() 入口 (23L) | `repl/repl-finish.ts` | 后续 |
| D-29.3.4 | ReplOptions + lazy client init (88L) | `repl/repl-options.ts` + `repl/repl-client-init.ts` | 后续 |

每个 sub-cmd 单独 ship, 单 commit, 跑过 0 new fail, 5 红线 1:1 保持. **预期 D-29.3
后 repl.ts ≤ 300L**.

---

## §7 验证总表 (3 commit 收口时跑)

| 检查项 | 命令 | 期望 |
| --- | --- | --- |
| 5 红线 1:1 | `git diff main..release/v1.0` | 5 段红线**逐字**保持 (允许行号浮动) |
| 测试 0 new fail | `pnpm test` | 628 pass / 5 skip / 0 fail (跟 d646af0 baseline 1:1) |
| 行数缩 | `wc -l packages/coding-agent/src/repl.ts` | 754L → ~550L |
| 模块文件数 | `ls packages/coding-agent/src/repl/*.ts` | 4 → 7 文件 (新增 3) |
| 公共 API | `git diff main..release/v1.0 -- src/index.ts src/repl.ts` | re-export 列表 1:1 (保 `runAgentTurn` export) |
| 3 commit 顺序 | `git log -3 --oneline` | D-29.2.1 → D-29.2.2 → D-29.2.3 |
| 推送 | `git push origin release/v1.0` | exit 0, 远端 3 commit 新增 |

---

## §8 Risks (拍板已知)

- **R-1**: `runAgentTurn` 抽到 `repl-agent-turn.ts` 后, 它依赖 `formatError` /
  `makeLlmSummarizeFn` / `appendStepSummary` 3 个 utilities. D-29.2.1 抽
  runAgentTurn 时, 这 3 utilities 还在 repl.ts, 走**相对路径 import 跨目录**
  (`./repl/repl-format-error.js`? — **不**, repl.ts 跟 `repl/` 同级, 抽到
  `repl/repl-agent-turn.ts` 后是 `../repl/repl-format-error.js`). 拍板:
  D-29.2.1 抽 runAgentTurn 时, 3 utilities 走 `../repl/repl-format-error.js`
  等相对路径 import, 跟 `repl-agent-turn.ts` 自己位置一致. D-29.2.2 抽完
  utilities 后, runAgentTurn 的 import 路径不变 (因为已经在 `repl/` 子目录
  内部互相引用). 0 改 D-29.2.2.
- **R-2**: 抽 `formatError` 仍保 module-private (不 re-export). 跟现行为一致.
  如果未来外部 caller 调, 再 re-export, 跟 D-29.1.2 `formatUsageStatus` 走过的
  re-export 形态一致.
- **R-3**: 单测不写 — 行为 1:1 保 628 既有测试, 红线行为靠集成测. 跟 D-29.1.1
  一致.

---

## §9 关联 sprint

- **D-29.1.1** (2026-06-07): signal-coordinator 抽 — 起点
- **D-29.1.2** (2026-06-07): session + UsageEmaState 抽
- **D-29.1.3** (2026-06-07): command-router 抽
- **D-29.1.4** (2026-06-07): trim 注释 (754L -20% from 947L)
- **D-29.2** (本 PR): utilities cluster 抽 (runAgentTurn + 3 utils) → ~550L
- **D-29.3+** (后续): startRepl 内 4 段 (line handler / close handler / finish /
  options) 抽 → ≤300L

---

## §10 Execution Modes

**本 PR 执行模式:** **直接执行 (manual)** — 不派 subagent. 抽 3 模块 0 改业务
机械操作, 跟 D-29.1.1 subagent 模式不同. D-29.1.1 subagent 当时是为了**学习**
抽法 (signal-coord 边界, 闭包图, 拍板红线), D-29.2 utilities 抽法形态已经被
D-29.1.1 ~ 1.3 锁死, 重复 subagent 没收益.

每个 commit 流程:
1. 写新文件 (机械迁移, 1:1 函数体)
2. 改 repl.ts (删函数体, 加 import, re-export if needed)
3. `pnpm test` 跑过
4. `git add` + `git commit -m "..."`
5. **不 push, 不 tag** — 3 commit 攒齐再 push

3 commit 攒齐后, 跑 §7 验证总表, push 到 `release/v1.0`. 跟 D-25 B5 / D-29.1
ship ritual 形态一致.
