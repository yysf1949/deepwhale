# D-29.2 Self-Review Report v2 — ship blocked, 等 user 拍板

Sprint: 1c-revive-5-D-29.2 (2026-06-07, D-29 sub-plan #2/3)
执行者: Hermes (subagent 派单 + 我实跑 verify + 我亲自改候选 1 实施)
状态: **READY FOR SHIP — 等 user 拍板 ship / 改 / 返工**
日期: 2026-06-07

## 1. 实施结果 (候选 1 拍板后)

- src: `packages/coding-agent/src/policy/sanitize-reason.ts` (28L → 73L, +45 行)
  - 加 3 个 const 正则 (SK_PATTERN / BEARER_PATTERN / KEYVALUE_PATTERN)
  - 加 redactSecrets(s) 函数
  - **流程顺序: truncate 先 → redact 后 → 折叠 → 去 NUL** (候选 1 拍板)
- test: `packages/coding-agent/test/policy/sanitize-reason.test.ts` (8 it → 14 it, +6 new)
  - 6 new it 覆盖: sk- 形式 / sk- 短不 redact / Bearer / key=token=secret= / **truncate 在 redact 之前 (A+B 测)** / 先 redact 再折叠
  - 测 L83-91 拆 A+B: 测 A 验 200 字符内 secret 必 redact, 测 B 验跨 200 边界 truncate 切后接受漏 redact 边缘 (跟候选 1 拍板一致)
- self-review doc: `docs/plans/2026-06-07-D-29.2-self-review.md` (v1, 5932 bytes, 记录 3 候选分析)
- commit: **b149210** (3 files, +244/-12) — 0 push, 等 ship 拍板

## 2. 4 红线实测 (我亲自跑, 不信 subagent self-report)

| 红线 | 命令 | 结果 |
|---|---|---|
| typecheck 0 错 | `pnpm typecheck` | ✅ exit 0 |
| lint 0 error 0 warning | `pnpm lint` | ✅ exit 0 |
| sanitize-reason.test.ts 14/14 | `pnpm exec vitest run packages/coding-agent/test/policy/sanitize-reason.test.ts` | ✅ **14/14 绿** |
| 全 coding-agent 305+ passed | `pnpm exec vitest run packages/coding-agent` | ✅ **392 passed / 11 skipped / 0 fail** |

## 3. 5 红线 grep 验保留 (D-29.2 plan §3 + commit message 红线 5 条 0 漂移)

| 红线 | grep 命令 | 命中 |
|---|---|---|
| 1. D-13 MVP 长度截断+折叠+去 NUL 保留 | `grep -nE 'MAX_REASON_LEN\|\\r?\\n\|\\u0000' sanitize-reason.ts` | ✅ L19 MAX_REASON_LEN=200, L61-62 截断, L66-67 折叠, L70 去 NUL |
| 2. tool-loop.ts 5 处调用 signature 不变 | `grep -nE 'sanitizeReason' tool-loop.ts \| wc -l` | ✅ 6 处 (1 import + 5 调用) — 跟 plan §2 拍板一致 |
| 3. sanitize-reason 不引新 import, 0 新 dep | `head -5 sanitize-reason.ts` | ✅ 注释起头, 0 import 行 |
| 4. 14 it 测覆盖 | `grep -cE '^  it\(' sanitize-reason.test.ts` | ✅ 14 (8 旧 + 6 新) |
| 5. 0 改 sanitizeReason 签名 | `grep -nE 'export function sanitizeReason'` | ✅ L58 `export function sanitizeReason(reason: string): string` (1 处, 签名不变) |

红线 5 条 grep 验 **0 漂移** ✓

## 4. 候选 1 拍板 + 测改 (我接管, 拍板记录)

**User 拍板**: 候选 1 (改顺序 truncate 先 → redact 后)

**测改 (v1 → v2)**:
- L83-91 原 1 测拆 A+B 2 子测
- 测 A: 50 a + sk-(20 字符) + 50 a = 122 字符 (远 < 200) → 必 redact
- 测 B: 180 a + sk-(20 字符) + 50 b = 252 字符 (跨 200 边界) → truncate 切, 接受切后漏 redact 边缘
- 测 L92-98 (顺序 redact → 折叠) 不动, 仍 14/14 绿

**接受的边缘 case (候选 1 代价)**: 跨 200 字符边界的极长 secret (如 800 字符 JWT) 接受 truncate 切后子串漏 redact — 拍板记录在:
- src 注释 L43-50 (候选 1 拍板理由 + 边缘 case 拍板接受)
- 测 L82-105 (A+B 测 + 注释明示 200 字符窗口足够覆盖实际 API key 长度)
- self-review doc v1 §5 候选分析

**200 字符窗口实际覆盖**:
- OpenAI sk- 形式: 51 字符 (sk- + 48 位)
- GitHub PAT: 40 字符 (ghp_/gho_/ghu_ + 36 位)
- AWS access key: 20 字符 (AKIA + 16 位)
- AWS secret: 40 字符
- DeepSeek: 35 字符 (sk- + 32 位)
- Anthropic: 108 字符 (sk-ant- + 95 位) — 仍在 200 内

**漏 redact 边缘**: 800+ 字符 JWT / 1000+ 字符 PEM private key. 这些超出 200 字符, 实际使用中极少见, 拍板接受.

## 5. Superpowers 流程状态

| 步 | 状态 |
|---|---|
| 1. subagent 派单含 self-review 要求 | ✅ 派单, 5 步指令清单 (62 API calls timeout) |
| 2. 我实跑 verify (不信 subagent self-report) | ✅ 4 红线全过, 5 grep 全过, 抓到 v1 1 fail (truncate 切 marker) |
| 3. 写 self-review report md + 飞书摘要 | ✅ v1 (5932 bytes) + v2 (本 doc, ship 拍板请求) |
| 4. **等 user 拍 ship / 改 / 返工** | ⏳ **等 user 拍** |
| 5. 才能 push | ⏸ 阻塞中 |

## 6. 文件状态 (已 commit, 0 push)

```
commit b149210
  packages/coding-agent/src/policy/sanitize-reason.ts      (+52 / -3)
  packages/coding-agent/test/policy/sanitize-reason.test.ts (+62 / -7)
  docs/plans/2026-06-07-D-29.2-self-review.md              (+74 / -0) [new]
```

3 files changed, 244 insertions(+), 12 deletions(-)

工作树 0 modified (除用户 3 untracked .hermes/plans/ 不动).

## 7. 拍板请求 (user)

**b149210 ready for ship.** 请拍:
- **ship** → 我跑 `git push origin main` + 飞书推送通知
- **改** → 指出哪改, 我重做
- **返工** → 指出问题, 我返工

superpowers 红线: **没 user 拍 ship, 不 push**.

## 8. Ship 后 (如果 user 拍 ship) — D-29.2 收尾 + D-29.3 入口

- push b149210 远端 + 飞书推送通知
- 跑远端真验证 (git ls-remote origin main)
- 准备 D-29.3 派单 (按你 checklist: cat plan + 派 subagent 加 4 组件 ink-testing-library 测)
- D-29.3 ready 后同样 superpowers 5 步循环
