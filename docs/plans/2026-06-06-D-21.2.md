# D-21.2: TUI/CLI 轻量升级 (横线分隔 + 状态栏增强)

**Sprint**: 1c-revive-2 (2026-06-06)
**选型**: 走 **1 轻量** (D-21.2 候选, 用户拍板 2026-06-06 17:00 选 1)
**Ship Ritual**: 1 plan + 1 commit cluster + 1 GitHub Release (v1.0.5)

## 背景

v1.0 TUI 是 D-20.3 P0-B "minimal" — 用 `╭─ ... ╮` 边框, 80 列终端看着局促, 状态栏是 dim 单行.
用户拍板 (2026-06-06 17:00): 走**轻量升级**, 不装 Ink (兼容 D-20.3 P0-B "不装新依赖" 红线),
80% 视觉提升 + 0 依赖风险.

**本轮范围 (sub-task 1+2) — 干**:
- 横线分隔 (header 上下 2 条 + status bar 上下 2 条)
- 状态栏增强 (formatTuiStatusBar helper, model 提到前面 + 窄终端截断)
- 1 新 test (验 ≥ 4 次 ─{3,} 出现)
- 1 旧 test 升级 (验 header 不再用 ╭─╮ 边框)

**本轮范围 (sub-task 3+4+5) — Defer P2, 留 D-22+**:
- sub-task 3: multi-line input (Defer — 跟 SIGINT 链冲突, 风险大)
- sub-task 4: 命令历史持久化 (Defer — 跟 D-19 复用红线冲突, 要 touch config)
- sub-task 5: 流式 token spinner (Defer — 现在 onChunk 直接 write, 已实时刷, 缺 spinner 动画)

## 改动清单

### tui.ts (1 文件)

**新增 2 helper** (D-21.2):
```ts
// D-21.2 (2026-06-06): 横线分隔, width 自适应 terminal columns (fallback 80, max 100).
function horizontalRule(width?: number): string

// D-21.2 (2026-06-06): 状态栏增强 — model 提到前面, 窄终端截断不溢出.
// 复用红线: 仍走 formatUsageStatus (4 字段), 不重建.
function formatTuiStatusBar(usage: string | null, model: string): string
```

**替换 2 处渲染**:
- 顶部 header: `╭─ ... ╮` / `│ ... │` / `╰─ ... ╯` → 横线 repeat + 单行 banner
- 状态栏: 上下加 `horizontalRule` wrap, 中间走 `formatTuiStatusBar`

**不动红线**:
- runToolLoop + staticToolPolicy (不绕过 ToolPolicy)
- createReplConfirm (D-19 串行化, 不重建 2 套)
- SessionWriter (不绕过 session audit)
- formatUsageStatus (4 字段风格统一)
- prompt / onChunk / confirm / session writer 路径

### tui-smoke.test.ts (1 文件, +1 test + 1 mockUsage + 1 旧 test 升级)

**+1 test (D-21.2 升级验收)**:
- 验走完 turn 后 stdout 出现 ≥ 4 次 ─{3,}
- 验状态栏文本必含 model 名 (formatTuiStatusBar 把 model 拼前面)

**1 旧 test 升级 (D-21.2 跟随)**:
- 把 `expect(out.data).toContain('╭─')` / `'╰─'` 换成 `expect(out.data).toMatch(/─{3,}/)`
- 加注释解释: v1.0 边框 → D-21.2 横线 repeat, 80 列更现代

**mock 加 usage 字段 (D-21.2 跟产线对齐)**:
- `makeMockStreamClient` 之前返 ChatResult 没 usage, 走 formatUsageStatus → null → else 分支只 1 条横线
- 加 mockUsage (100/50/150 + cache 80), 让 status bar 走 if 分支 (上下 2 条横线)
- 跟 deepseek-client 真实形态对齐, 数字随便

## 验证

- `pnpm test` 539 passed | 20 skipped (从 538 → 539, +1 新 test, 全过)
- `pnpm lint` 0 警告
- `tsc -b` 0 错

## 拍板红线

| 红线 | 拍板 | 决定 |
|---|---|---|
| 选 1/2/3 (轻量/Ink/双形态) | 选 1: 轻量 | ✅ 不装新依赖, 兼容 D-20.3 P0-B |
| sub-task 1+2 范围 | 全干 | ✅ 横线 + 状态栏, ~1 小时 |
| sub-task 3+4+5 | Defer P2, 留 D-22+ | ✅ 不抢拍, 风险控制 |
| mock 加 usage 是否影响旧 test | 验过 538 + 1 全过, 无副作用 | ✅ 其它 test 不依赖 status bar 内容 |

## 范围 & 风险

**改动**: 1 commit, 6 文件 +113/-11
- packages/coding-agent/src/modes/tui.ts (+50/-11)
- packages/coding-agent/test/modes/tui-smoke.test.ts (+63/-0, 1 新 test + 1 mockUsage + 1 旧 test 升级)
- 4 package.json (version 1.0.4 → 1.0.5)

**风险评估**:
- helper 不动 readline / SIGINT / confirm / session 路径
- header 改 `╭─╮` → `─` repeat: 视觉变, **功能不变**, 1 旧 test 升级期望
- 状态栏加横线 wrap: 视觉变, **功能不变**
- mockUsage 字段: 让 mock 走 if 分支, 之前 mock 返 `usage: undefined` 走 else 路径 (1 条横线), 现在 if 路径 (2 条横线), 不影响其它 test (它们只验 stdout 含 chat 内容 / tool 名, 不数横线)

## 不做 (Backlog)

| 任务 | 状态 | 备注 |
|---|---|---|
| Multi-line input (\\ 续行 / 自动 wrap) | Defer D-22+ | SIGINT 链冲突, 风险大 |
| 命令历史持久化 (~/.deepwhale/tui-history) | Defer D-22+ | 跟 D-19 复用红线冲突 |
| 流式 token spinner (转动 dots) | Defer D-22+ | 现在 onChunk 实时刷, 缺动画 |
| Ink 升级 (中型) | Defer | 用户选 1, 不抢拍 |
| Electron/Tauri 双形态 (重型) | Defer | Sprint 2+ |

## Live State

- Repo: 待 commit (本 plan 文件) → force push release/v1.0
- GitHub Release: v1.0.5 (Latest) + 4 tarball
- npm registry: 仍 1.0.1 (security key 2FA 拦 publish, 走 tarball workaround)
- 飞书 DM: 推送通知待发
