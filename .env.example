# deepwhale 项目根 .env 模板
# 用法:
#   cp .env.example .env
#   chmod 600 .env          # 红线 4: 文件权限
#   vim .env                # 填 key
#
# 加载机制 (Sprint 1c-revive-2-D-7, 2026-06-04):
#   vitest 启动时自动调 loadProjectEnv() (packages/coding-agent/src/env/load-project-env.ts)
#   CLI 入口 bin/deepwhale.js 同样会调
#   只补缺不覆盖 (process.env[key] ??= value) → shell export / CI set 永远最高优先
#
# 红线 (跟 README "integration tests" 一致):
#   - .env 在 .gitignore 里, 永**不**进 commit
#   - .env.example 是模板, 不会含真 key, 可以进 commit
#   - test 不会读 .env, 只看 process.env

# --- 真接开关 ---
# 0 (默认) → integration test 全部 it.skip, 纯离线
# 1        → 真接 LLM (走真 API, 会花 ¥)
INTEGRATION=0

# --- DeepSeek OAI shim 真接 ---
# 申请: https://platform.deepseek.com/api_keys
# **不填** = 留空字符串 = DeepSeek 真接测 it.skip (不会 401/假请求)
# 必填: 替换成真 key, e.g. `DEEPSEEK_API_KEY=sk-xxx`
DEEPSEEK_API_KEY=

# --- Anthropic shim 真接 (Sprint 1c-revive-2-B 起) ---
# 注意: 这里是 ANTHROPIC_AUTH_TOKEN 不是 ANTHROPIC_API_KEY
# 跟 Anthropic SDK 官方环境变量名一致
# **不填** = 留空字符串 = Anthropic 真接测 it.skip (不会 401/假请求)
# 必填: 替换成真 key, e.g. `ANTHROPIC_AUTH_TOKEN=sk-ant-xxx`
ANTHROPIC_AUTH_TOKEN=
